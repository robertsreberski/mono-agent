{
  "name": "mono-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Small agent framework building blocks under the @worklab-ai scope.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build-workspaces.mjs --script build",
    "typecheck": "node scripts/build-workspaces.mjs --script typecheck",
    "test": "node scripts/build-workspaces.mjs --script test"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
