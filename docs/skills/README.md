# Skills moved

The `mono-agent-composer` skill now lives at
[`packages/agent-app/skills/mono-agent-composer/`](../../packages/agent-app/skills/mono-agent-composer/)
so it ships with the published `@mono-agent/agent-app` package.

Install it into Claude Code and Codex with:

```bash
mono-agent install-skill
```
