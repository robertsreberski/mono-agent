---
// mono-agent marketing site — single static page for https://mono-agent.dev/.
//
// Copy rules for this file: every claim must already be true of the published
// baseline or be explicitly framed as configuration. Never advertise
// source-only capabilities (console projects/tags, durable subagents, usage
// meters — see docs/reference/release-status.md), never promise setup speed,
// security, or availability, and never present the decorative brand artwork
// as a product screenshot.
const SITE_URL = "https://mono-agent.dev";
const GITHUB_URL = "https://github.com/robertsreberski/mono-agent";
const DOCS_URL = "https://mono-agent-docs.vercel.app/";
const RELEASE_STATUS_URL = `${DOCS_URL}reference/release-status/`;

const TITLE = "mono-agent — Local-first AI workspace for coding and research";
const DESCRIPTION =
  "mono-agent is a local-first, config-first AI workspace for coding, research, and automation. One mono-agent.config.json defines your agent — models, tools, skills, memory, and channels. Open source (GPL-3.0).";
const OG_IMAGE = `${SITE_URL}/og-1200x630.jpg`;

const jsonLd = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebSite",
      "@id": `${SITE_URL}/#website`,
      url: `${SITE_URL}/`,
      name: "mono-agent",
      description: DESCRIPTION,
    },
    {
      "@type": "Organization",
      "@id": `${SITE_URL}/#organization`,
      name: "mono-agent",
      url: `${SITE_URL}/`,
      logo: OG_IMAGE,
      sameAs: [GITHUB_URL],
    },
  ],
};
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{TITLE}</title>
    <meta name="description" content={DESCRIPTION} />
    <link rel="canonical" href={`${SITE_URL}/`} />
    <meta name="theme-color" content="#101311" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="stylesheet" href="/styles.css" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="mono-agent" />
    <meta property="og:title" content={TITLE} />
    <meta property="og:description" content={DESCRIPTION} />
    <meta property="og:url" content={`${SITE_URL}/`} />
    <meta property="og:image" content={OG_IMAGE} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta
      property="og:image:alt"
      content="mono-agent social card: the brand name and “Local-first AI workspace for coding and research” beside the stacked-slab artwork."
    />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={TITLE} />
    <meta name="twitter:description" content={DESCRIPTION} />
    <meta name="twitter:image" content={OG_IMAGE} />
    <meta
      name="twitter:image:alt"
      content="mono-agent social card: the brand name and “Local-first AI workspace for coding and research” beside the stacked-slab artwork."
    />

    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>

    <header class="site-header">
      <div class="wrap">
        <a class="brand" href="#top" aria-label="mono-agent — back to top">
          <img src="/favicon.svg" alt="" width="30" height="30" aria-hidden="true" />
          <span>mono-agent</span>
        </a>
        <nav class="site-nav" aria-label="Sections">
          <ul>
            <li><a href="#use-cases">Use cases</a></li>
            <li><a href="#why">Why mono-agent</a></li>
            <li><a href="#start">Get started</a></li>
            <li><a href="#faq">FAQ</a></li>
          </ul>
        </nav>
        <div class="header-cta">
          <a class="link-docs" href={DOCS_URL}>Docs</a>
          <a class="btn btn-primary btn-small" href={GITHUB_URL}>GitHub</a>
        </div>
      </div>
    </header>

    <main id="main">
      <section class="hero" id="top" aria-labelledby="hero-heading">
        <div class="wrap hero-grid">
          <div class="hero-copy">
            <span class="eyebrow"><span class="status-dot" aria-hidden="true"></span> A local-first AI workspace</span>
            <h1 id="hero-heading">Your agents. Your models. Your workspace.</h1>
            <p class="lede">
              A folder becomes an agent. A conversation becomes work.
              Code, research, and automate with the models you choose —
              in a workspace you control.
            </p>
            <div class="hero-actions">
              <a class="btn btn-primary" href={GITHUB_URL}>Get the code on GitHub</a>
              <a class="btn btn-secondary" href={DOCS_URL}>Read the docs</a>
            </div>
            <p class="hero-meta">Open source. Bring your own models. Make it yours.</p>
          </div>
          <figure class="hero-art" aria-hidden="true">
            <picture>
              <source type="image/webp" srcset="/hero-640.webp 640w, /hero-960.webp 960w, /hero-1440.webp 1440w" sizes="(max-width: 959px) 100vw, (max-width: 1250px) 80vw, 1000px" />
              <img
                src="/hero-1440.jpg"
                srcset="/hero-640.jpg 640w, /hero-960.jpg 960w, /hero-1440.jpg 1440w"
                sizes="(max-width: 959px) 100vw, (max-width: 1250px) 80vw, 1000px"
                width="1536"
                height="1024"
                alt=""
                fetchpriority="high"
              />
            </picture>
            <figcaption class="visually-hidden">Decorative brand illustration, not a product screenshot.</figcaption>
          </figure>
        </div>
        <div class="wrap hero-footnote"><span>01 / YOUR WORK, YOUR WAY</span><span>One config. Many possibilities. <span aria-hidden="true">↘</span></span></div>
      </section>

      <section class="capability-strip" aria-label="Core capabilities">
        <div class="wrap"><span>Built around your choices</span><ul><li>Local &amp; cloud models</li><li>Persistent conversations</li><li>Skills &amp; MCP</li><li>Optional memory</li></ul></div>
      </section>

      <section class="section" id="use-cases" aria-labelledby="use-cases-heading">
        <div class="wrap">
          <span class="section-label">Use cases</span>
          <h2 id="use-cases-heading">Less switching.<br />More doing.</h2>
          <p class="section-intro">For the code you need to ship, the question you need to answer,
            and the routine you would rather not repeat.</p>
          <ol class="use-cases">
            <li>
              <span class="case-index">01 / BUILD</span>
              <h3>From stuck<br />to shipped.</h3>
              <p>Give your agent a codebase. Trace bugs, review changes, and run tests
                without losing the conversation when you close the tab.</p>
              <div class="example-prompt"><span>Try asking</span><p>“Find why this test fails. Explain the cause, then propose a fix.”</p></div>
              <a href={`${DOCS_URL}getting-started/quickstart/`}>Start with a project <span aria-hidden="true">↗</span></a>
            </li>
            <li>
              <span class="case-index">02 / RESEARCH</span>
              <h3>From scattered<br />to understood.</h3>
              <p>Bring your notes, documents, and questions. Compare source material,
                develop an argument, and turn a research folder into a useful draft.</p>
              <div class="example-prompt"><span>Try asking</span><p>“Compare these reports. Where do they disagree, and what is missing?”</p></div>
              <a href={`${DOCS_URL}memory/`}>Explore optional memory <span aria-hidden="true">↗</span></a>
            </li>
            <li>
              <span class="case-index">03 / AUTOMATE</span>
              <h3>From repetitive<br />to routine.</h3>
              <p>Schedule a digest, handle a webhook, or reach your agent through
                Telegram and Slack. Configure the routine once, then let it run.</p>
              <div class="example-prompt"><span>Example workflow</span><p>A scheduled morning digest from the sources and tools you configure.</p></div>
              <a href={`${DOCS_URL}channels/cron/`}>Explore scheduled work <span aria-hidden="true">↗</span></a>
            </li>
          </ol>
        </div>
      </section>

      <section class="section" id="why" aria-labelledby="why-heading">
        <div class="wrap">
          <span class="section-label">Why mono-agent</span>
          <h2 id="why-heading">Your setup.<br />Not someone else’s box.</h2>
          <p class="section-intro">
            What you set up is something you can read, review, version, and
            move — not host code you maintain.
          </p>
          <div class="config-story">
            <div class="config-copy"><span class="section-label">Small file. Big possibility.</span><h3>The agent starts<br />with your choices.</h3><p>Choose a model. Give it a workspace. Add the capabilities you need. Your configuration stays readable, reviewable, and yours.</p><a href={`${DOCS_URL}runtime/`}>Explore model configuration <span aria-hidden="true">↗</span></a></div>
            <figure class="config-example"><figcaption><span aria-hidden="true">&#123; &#125;</span> mono-agent.config.json <span class="example-label">Minimal example</span></figcaption><pre><code>{`{
  "runtime": {
    "model": "openai-codex:gpt-5.6-terra",
    "workspace": "."
  },
  "context": {
    "identityPath": "./IDENTITY.md"
  }
}`}</code></pre><p>A starting point, not the whole setup. Add your identity file and configure provider access.</p></figure>
          </div>
          <ul class="points">
            <li>
              <h3>One file defines the agent</h3>
              <p>
                <code>mono-agent.config.json</code> holds the model routes,
                tools, skills, memory, and channels — configuration you can
                version, review, and reuse across folders instead of host
                code you maintain.
              </p>
            </li>
            <li>
              <h3>Local-first, not local-only</h3>
              <p>
                Run artifacts stay on your machine, credentials stay in a dotenv
                file or your provider's own auth store, and model requests go
                wherever you point them — a subscription or API provider, or a
                fully local model.
              </p>
            </li>
            <li>
              <h3>Reach it where you work</h3>
              <p>
                The browser console, Telegram, Slack, webhooks, cron schedules,
                and an OpenAI-compatible endpoint are transports for the same
                agent, each enabled with its own config flag.
              </p>
            </li>
            <li>
              <h3>Extend without writing a host</h3>
              <p>
                Add instruction sets as skills, attach MCP servers for extra
                tools, and narrow the tool surface with policy — plus an opt-in
                sandbox that confines tool subprocesses to the roots you allow.
              </p>
            </li>
          </ul>
          <div class="note">
            <p>
              <strong>Two defaults worth knowing.</strong> Tools are allow-all
              by default and the sandbox is opt-in, so narrow
              <code>tools.allowedTools</code> before exposing an agent. The web
              console has no application login — run it on loopback and read
              the safety docs before widening access.
            </p>
          </div>
        </div>
      </section>

      <section class="section" id="start" aria-labelledby="start-heading">
        <div class="wrap">
          <span class="section-label">Get started</span>
          <h2 id="start-heading">From empty folder to working agent</h2>
          <p class="section-intro">
            A real workspace, not another hosted account. You’ll need Node.js 24.15+
            and access to a model. Guided setup is not instant: it checks your routes
            with real model calls, which can take several minutes and count as usage.
          </p>
          <ol class="steps">
            <li>
              <h3>Install the CLI</h3>
              <p>Requires Node.js 24.15.0 or newer. No pnpm needed.</p>
              <pre><code>npm i -g create-mono-agent</code></pre>
            </li>
            <li>
              <h3>Create the agent folder</h3>
              <p>
                Bare <code>init</code> on a terminal is the guided wizard: it
                names the agent, writes its role, and proves each model route.
                Any flag scaffolds the files only.
              </p>
              <pre><code>mkdir my-agent
cd my-agent
mono-agent init</code></pre>
            </li>
            <li>
              <h3>Start it, then open the workspace</h3>
              <p>
                Validate and start the agent, then run the browser console in
                its own terminal and open the URL it prints.
              </p>
              <pre><code>mono-agent validate
mono-agent start
mono-agent web run --loopback</code></pre>
            </li>
          </ol>
          <div class="note">
            <p>
              <strong>Published vs. source.</strong> The docs describe the
              current source; the published npm release can lag behind it. The
              <a href={RELEASE_STATUS_URL}>release status page</a> lists exactly
              what is in the release and how to run a source build instead.
            </p>
          </div>
        </div>
      </section>

      <section class="section" id="faq" aria-labelledby="faq-heading">
        <div class="wrap">
          <span class="section-label">FAQ</span>
          <h2 id="faq-heading">Short answers, honestly</h2>
          <dl class="faq">
            <div>
              <dt>What does “local-first” actually mean here?</dt>
              <dd>
                Your agent files and run artifacts are stored on your machine
                by default. The models, tools, and channels you configure may
                send the context they need to their own services, so read what
                each one transmits before you enable it. Preferring a model on
                your own hardware keeps even inference local.
              </dd>
            </div>
            <div>
              <dt>Which models can I use?</dt>
              <dd>
                Supported <code>&lt;provider&gt;:&lt;model&gt;</code> routes include
                subscription and API providers such as OpenAI Codex and
                Anthropic, plus fully local providers like Ollama or LM Studio.
                Ordered fallbacks keep a turn alive when the primary route fails.
              </dd>
            </div>
            <div>
              <dt>Is the web console safe to expose?</dt>
              <dd>
                Treat it as single-owner with no application login: anyone who
                can reach it can operate your agents. Keep it on loopback
                unless you have a reason not to, and read the console security
                docs first.
              </dd>
            </div>
            <div>
              <dt>What does it cost?</dt>
              <dd>
                mono-agent itself is free, open-source software under GPL-3.0.
                Cloud usage follows your provider’s subscription or API pricing,
                including setup calls. Local models use your own hardware.
              </dd>
            </div>
            <div>
              <dt>Where is the full documentation?</dt>
              <dd>
                The complete guides, configuration reference, and security model
                live on the documentation site — start there when the config
                file gets ambitious.
              </dd>
            </div>
          </dl>
        </div>
      </section>

      <section class="closing" aria-labelledby="closing-heading">
        <div class="wrap">
          <span class="section-label">Get the code</span>
          <h2 id="closing-heading">Make room for<br /><span>your kind of agent.</span></h2>
          <p>
            Start with a folder. Choose your models. Build a workspace
            around the way you think and work. No mono-agent account required.
          </p>
          <div class="hero-actions">
            <a class="btn btn-primary" href={GITHUB_URL}>Get the code on GitHub</a>
            <a class="btn btn-secondary" href={DOCS_URL}>Read the docs</a>
          </div>
        </div>
      </section>
    </main>

    <footer class="site-footer">
      <div class="wrap">
        <span>mono-agent — open source under GPL-3.0-only.</span>
        <nav aria-label="Project links">
          <ul>
            <li><a href={GITHUB_URL}>GitHub</a></li>
            <li><a href={DOCS_URL}>Documentation</a></li>
            <li><a href={RELEASE_STATUS_URL}>Release status</a></li>
          </ul>
        </nav>
      </div>
    </footer>
  </body>
</html>
