interface:
  display_name: "Worktree Feature"
  short_description: "Run isolated feature work through safe cleanup"
  default_prompt: "Use $worktree-feature to run isolated mono-agent feature work through a PR and API-confirmed post-merge cleanup."
