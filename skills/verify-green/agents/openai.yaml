interface:
  display_name: "Verify Green"
  short_description: "Run the mono-agent verification gate"
  default_prompt: "Use $verify-green to run the mono-agent verification gate before claiming a change is green."
