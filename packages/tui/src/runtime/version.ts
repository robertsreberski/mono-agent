/**
 * Bundled package version string for the help overlay. Updated manually
 * when the package version changes; we avoid importing package.json at
 * runtime so the dist remains a self-contained ESM module.
 */
export const TUI_PACKAGE_VERSION = "0.1.0";
