# @mono-agent/session-web

Legacy read-only Session Recorder backend and PWA. It still ships for
programmatic embedders while retirement is pending, but it no longer has a
`mono-agent sessions` launcher. Use `mono-agent tui` for recorded-run replay or
`mono-agent web` for the supported browser console.

## Category

<!-- package-metadata:start -->
<!-- Generated by scripts/generate-package-docs.mjs. Do not edit by hand. -->

Category: `operator-surface`
Tier: `core`
Catalog responsibility: Maintains the legacy read-only Session Recorder PWA for programmatic embedders, combining discovered agents' recorded and live runs while retirement remains pending.

<!-- package-metadata:end -->

## Responsibility

- Discover instances (registry `listTraceSources`/`mergeTraceSources`), exposing
  name, cwd, `artifactDir`, process health, independent typed memory health, and
  the optional `live` SSE endpoint.
- Read recorded runs per instance and map them to the UI `Session` model
  (`mapRunToSession`, from `@mono-agent/observability`).
- Watch artifact dirs (run-level) and connect to operator-adapter live SSE (sub-run);
  fold both into one live session model.
- Serve the built PWA (`express.static`), a JSON API, and a browser SSE stream.

This package is read-only by construction: it never submits, cancels, or
modifies an agent turn.

## Install / Usage

There is no config-first or CLI startup path. Install and start it explicitly
only when maintaining an existing programmatic integration:

```bash
pnpm add @mono-agent/session-web
```

```ts
import { startSessionWebServer } from "@mono-agent/session-web";

const server = await startSessionWebServer({
  registryDirs: ["/Users/me/.mono-agent/trace-sources"],
  host: "127.0.0.1",
  port: 0,
  // Defaults to agent runs only. Uncomment to include memory-maintenance runs.
  // includeMemory: true,
});
console.log(`Open ${server.url}`);
await server.stop();
```

The handle's `url` is the actual bound browser root, including the real port and
trailing slash (for example `http://127.0.0.1:51234/` when `port: 0`). Use that
value rather than reconstructing a URL from requested bind options. Loopback on
port `4599` is the default. A non-loopback bind requires `allowNonLoopback: true`
and an `authToken`; `/api/*` and `/api/stream` require `Authorization: Bearer
<token>`. The PWA consumes SSE with authenticated `fetch`, so the bearer never
appears in the stream URL. A configured token is also enforced when the server
binds loopback.

For each browser origin, the PWA mirrors a captured or manually entered token
into current-tab `sessionStorage` as `mono-agent.session-web.authToken` and
persistent `localStorage` as `mono-agent.session-web.authToken.persisted`. The
persistent copy survives page reloads, tab or browser closes, and browser
restarts. On initial page load or after a manual reload, a JSON API 401/403
exposes the authentication form. If the server token changes while the page
remains open, a stream 401/403 stays in the reconnecting state and retries with
the stored token instead of exposing the form. Reload the page to expose the
form; enter the current token and choose **Retry**, or choose **Clear** first to
remove both browser-storage copies. Clearing the site's browser storage/site
data also removes them; closing the browser alone does not sign out.

For direct LAN/Tailscale access, bind the server to a non-loopback host with the
`startSessionWebServer` options; Tailscale Serve is not a prerequisite:

```ts
await startSessionWebServer({
  registryDirs: ["/Users/me/.mono-agent/trace-sources"],
  host: "0.0.0.0",
  port: 4599,
  allowNonLoopback: true,
  authToken: "<strong-stable-token>",
});
```

Startup advertises concrete loopback, private-LAN, and Tailscale IPv4 URLs, not
`0.0.0.0`. Direct Tailscale HTTP is protected by the tailnet transport; plain
LAN HTTP is not encrypted. Use Tailscale Serve (or another HTTPS proxy) only
when HTTPS-dependent browser behavior such as installable/offline PWA support
is required.

Run lists and the initial browser SSE snapshot are summary-only and step-less.
Persisted, bounded run timelines are read lazily from `/api/sessions/:sourceId/:runId` when a
detail view opens.
`/api/sessions` supports `instance`, `limit`, and `offset` query parameters and
returns page metadata (`total`, `offset`, `limit`, `hasMore`) so the browser can
load older history without replaying already-loaded rows. Stale `running`
summaries are projected as `stalled` in the web surface instead of being treated
as live.
Instance metadata includes the source timezone when it is discoverable; the PWA
uses that timezone for per-instance run lists and details, and falls back to the
viewer locale/timezone for mixed-instance views. Failure summaries preserve
`failureKind`, error text, and provider failover attempts so failed runs are
inspectable from the list and detail views.
Memory-maintenance runs are hidden by default across disk history, watched
updates, API responses, browser SSE frames, and live folds; pass
`includeMemory: true` to opt them back in.

Instance cards render memory separately from process/live health: healthy is
green, in-progress teal, degraded amber, unhealthy red, and unknown/off gray.
Accessible labels/tooltips contain only the status and stable closed issue
codes. The aggregator compares typed `memoryHealth` values during discovery;
a memory-only registry change emits an `instances` browser SSE frame, so the
badge refreshes without a new run or process-health transition.
Backend-discriminated normalization prevents impossible remote/absent combinations
from rendering green, and overlapping registry reconciles are single-flight with
one bounded trailing replay so an older read cannot replace newer health.
The browser also revalidates the closed, canonical issue list and partial count
relationships so malformed raw runtime data fails closed to `unknown` without
surfacing provider text or unknown keys.

## Architecture

### Data flow

1. Discovery merges trace-source registries into typed instance records and
   accepts only trusted loopback live-relay URLs.
2. History readers page bounded recorded-run summaries and lazily map selected
   artifacts to the shared observability `Session` model.
3. The aggregator watches artifact directories and folds trusted live SSE
   sub-run frames into the same in-memory projection. Reconciliation is
   single-flight with one bounded trailing pass.
4. The server exposes read-only JSON routes plus `/api/stream`, then serves the
   bundled PWA at the returned root URL.

### Package structure

| Source area | Responsibility |
| --- | --- |
| [`discovery.ts`](https://github.com/robertsreberski/mono-agent/blob/main/packages/session-web/src/discovery.ts) | Registry discovery, trusted live URL parsing, and live-key resolution. |
| [`history.ts`](https://github.com/robertsreberski/mono-agent/blob/main/packages/session-web/src/history.ts) | Bounded artifact listing and lazy detail reads. |
| [`live-client.ts`](https://github.com/robertsreberski/mono-agent/blob/main/packages/session-web/src/live-client.ts) | Agent-side read-only SSE subscription. |
| [`aggregator.ts`](https://github.com/robertsreberski/mono-agent/blob/main/packages/session-web/src/aggregator.ts) | Recorded/live reconciliation, stale projection, watches, and browser subscribers. |
| [`server.ts`](https://github.com/robertsreberski/mono-agent/blob/main/packages/session-web/src/server.ts) | Safe binding, bearer checks, read-only API/SSE, and PWA serving. |
| [`webapp/`](https://github.com/robertsreberski/mono-agent/tree/main/packages/session-web/webapp) | Legacy Session Recorder PWA and its isolated dependency graph. |

## Public API

### Start here

| API | Use it for |
| --- | --- |
| `startSessionWebServer` | Start the programmatic-only read-only server and receive its actual URL. |
| `discoverWebInstances` | Read and normalize instances from trace-source registries. |
| `liveBaseUrlFromMetadata` / `resolveLiveApiKey` | Resolve a trusted live relay without copying secrets into registry metadata. |
| `BrowserStreamFrame` / `WebInstance` | Implement another read-only client of the browser stream. |
| Re-exported `Session*` types | Consume the canonical recorded-run model owned by observability. |

<!-- public-api-inventory:start -->
<!-- Generated by scripts/generate-public-api-docs.mjs. Do not edit by hand. -->

Every symbol exported by each public code entrypoint is listed below.

**`@mono-agent/session-web`**

```text
BrowserStreamFrame
DiscoverWebInstancesOptions
DiscoveredWebInstance
Session
SessionOutcome
SessionStep
SessionStepUsage
SessionThink
SessionToolCall
SessionTotals
SessionWebServerHandle
StartSessionWebServerOptions
WebInstance
defaultTraceRegistryDir
discoverWebInstances
liveBaseUrlFromMetadata
resolveLiveApiKey
startSessionWebServer
```

<!-- public-api-inventory:end -->

## Dependency Boundary

Depends only on `core` + `observability`: `@mono-agent/agent-contracts`,
`@mono-agent/observability`. Plus `express`. It reaches
operator-adapter live endpoints **over HTTP only** — it does not (and, per the
architecture rule, may not) depend on that `communication` package. The browser
SPA lives in the isolated `webapp/` sub-project (its own lockfile), built to
`webapp/dist` and served statically.

## What This Package Does Not Own

- The agent-side broadcast tap or the `live` channel driver.
- Turn-driving / any write path to an agent (read-only by construction).
- The recorder, the trace-source registry writer, or run persistence.
- A CLI launcher or managed-service lifecycle; the former `sessions` command was
  removed and full package retirement is tracked separately.

## Related Documentation

- [Session Recorder deprecation status](https://mono-agent-docs.vercel.app/reference/deprecations/#removed-surfaces)
- [Terminal UI and recorded-run replay](https://mono-agent-docs.vercel.app/observability/tui/)
- [Always-on web console](https://mono-agent-docs.vercel.app/observability/web-console/)
- [Artifacts and traces](https://mono-agent-docs.vercel.app/observability/artifacts-and-traces/)

## Verification

```sh
pnpm --filter @mono-agent/session-web run build
pnpm --filter @mono-agent/session-web run typecheck
pnpm --filter @mono-agent/session-web run test
```
