# Dead-code ledger

**Scope:** consolidates §3 (Dead code) and §4 (Deprecation & legacy) from all 24 raw territory artifacts (`_raw/A1`…`_raw/G1`) with the corrections from the 10 adversarial verifier passes (`_raw/verified/V1`…`V10`). Only **PROVEN** entries (independently grepped by a verifier against the repo, `demos/`, `scripts/`, `website/`, `docs/`, and both live instances `~/personal-agent` / `~/a8c-agents`, excluding `node_modules`/`dist`) are listed as confirmed in §A/§B. Nothing refuted was silently dropped — refutations are preserved in §D so a future cleanup pass doesn't re-delete something load-bearing on a stale hunch, and open questions are preserved in §C rather than asserted either way.

None of the items below are v1 freeze-blockers (the freeze-blocker verdicts live in `parts/*` and `security.md`); this document is purely the removability/legacy inventory.

---

## A. Confirmed removable now

34 items. "Territory" cites the raw artifact(s) + verifier that proved it; "Proof" is the verifier's independent grep/reproduction, not the original auditor's claim.

### A.1 — Repo: dead exports, `packages/agent-app`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 1 | `wizard/run.ts:108-118,336-388` — `ModelRepairOutcome` / `runModelRepairWizard`, plus the two re-exports at `wizard/index.ts:35-36` | A1 · V1 | `grep -rln runModelRepairWizard` (excl. `node_modules`/`dist`) → only `wizard/run.ts`, `wizard/index.ts`, `wizard-run.test.ts`, `cli-first-run-state.test.ts`. Zero hits in both live instances. **V1 strengthens**: `agent-app/package.json` `exports` map exposes only `.` — the symbol is unreachable even by external npm consumers, so the auditor's "keep as external API" hedge is moot. | Delete the function, its type, the two re-exports, and fold/delete the dedicated test blocks. **Acceptance:** repo-wide + live-instance grep for the symbol returns 0 (excl. this ledger); package builds/tests green. |
| 2 | `init.ts:845` — `secretEnvLockPathFor` | A1 · V1 | `grep -rln secretEnvLockPathFor` → `init.ts` + `secret-env.test.ts` only; 0 in live instances; absent from top-level `index.ts` and the `exports` map. | Drop the `export` keyword (make internal) or delete outright. **Acceptance:** symbol no longer appears in any public surface; existing test still compiles (adjust import path if kept internal). |
| 3 | `provider-setup.ts:251` — `piLoginCommandLine` | A2 · V1 | `grep -rln piLoginCommandLine` (excl. dist) → only the definition. Near-duplicate of `piAuthRecoveryCommand(provider)`. 0 in live instances. | Delete, or inline at any future call site. **Acceptance:** grep 0 outside `dist/`. |
| 4 | `readiness-probe.ts:563` — `runReadinessProbe` (public single-route API) | A2 · V1 | `grep -rn "runReadinessProbe\b"` → definition + its own test only; `cli.ts` exclusively drives `runAllRouteReadinessProbe` via a local `runReadinessProbeWithSpinner` wrapper. **V1 amends disposition**: not in `index.ts`, not in the `exports` map → no external-consumer risk, cleanly deletable (the auditor's "keep for npm consumers" hedge does not survive verification). | Delete; fold any residual coverage value into `runAllRouteReadinessProbe`'s test. **Acceptance:** grep 0 outside `dist/`/own test. |
| 5 | `background.ts:994-996` — `findInstance` (singular) | A3 · V1 | `grep -n "findInstance("` in `background.ts` → one hit, the definition itself. Superseded everywhere by `findInstances`. | Delete. **Acceptance:** single remaining grep hit is `findInstances` (plural). |
| 6 | `posted-message-index.ts:128` — `compactPostedMessageIndex` | A4 · V2 | `grep -rn compactPostedMessageIndex --include=*.ts .` (excl. node_modules/dist) → 2 files: definition + its own test. Zero production call sites; not re-exported. Left un-wired, the `posted-message-index.jsonl` grows unbounded (Slack-send-heavy agents; `lookupProducingConversation`'s linear scan degrades with it). | Either wire a call site (mirror `pruneTraceSources` in `web-command.ts`/`tui-command.ts`, e.g. at channel start) or delete the tested-but-unwired safety valve — leaving it half-built is the worst of the two options. **Acceptance:** either the `.jsonl` is observably bounded by a test, or the symbol is gone. |
| 7 | `memory-recall.ts:330-398` — `createMemoryRecallRuntimeExtension`, `memoryRecallMcpEnv`, `memoryRecallMcpServerSpec` | A6 · V2 | `grep -rn createMemoryRecallRuntimeExtension` (excl. dist) → **only its own definition** — not even referenced by its own test. Production wiring (`configured-agent.ts`) exclusively uses `createSharedMemoryRecallRuntimeExtension` from `memory-retrieval.ts`. Not re-exported. Live instances: 0 hits. | Delete all three. Do **not** confuse with the separately load-bearing `mono-agent-memory-recall` published bin (`memory-recall-main.ts`'s `memoryRecallSettingsFromEnv`/`createRecallStore`, which `package.json:22` genuinely depends on) — that stays. **Acceptance:** grep 0 outside `dist/`; published bin's own tests unaffected. |

### A.2 — Repo: dead exports, `packages/memory`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 8 | `distill.ts:22` — `distill()` | B1 · V3 | `grep -rn "\bdistill\b" --include=*.ts` (excl. `__tests__`/`dist`) → only definition + `index.ts:101` re-export. | Delete, or explicitly document as a legacy composable primitive if kept for reference. **Acceptance:** grep 0. |
| 9 | `entities.ts:103` — `extractEntities()` | B1 · V3 | `grep -rn extractEntities` → only definition + `index.ts:106` re-export. | Delete, or document as legacy. **Acceptance:** grep 0. |
| 10 | `capture-intake.ts:1972` — `compareLocated()` | B1 · V3 | `grep -in comparelocated` → single hit (the definition); `nextPendingRuntimeId`/`pruneResolved` reimplement equivalent sort logic inline instead. | Delete. **Acceptance:** grep 0. |
| 11 | **Capture-queue subsystem — AMENDED/enlarged scope (V3 NEW-1).** `capture.ts:29-31` `captureTurn` (non-strict), `capture-batch.ts:45-79` `extractCapturePlan` (non-strict), plus `store.ts` `initializeCaptureQueue()`, `captureQueue`, `CaptureJob`, `activeCaptureController`, `captureAccepted` | B1 · V3 | The raw audit (B1-F2) framed only `captureTurn`/`extractCapturePlan` as dead, reachable "only via `BujoMemoryStore.capture()`." **V3 proves the reachability claim itself was incomplete and the dead surface is larger**: the real feeder is the published optional `MemoryStore.scheduleCapture` contract method (`agent-contracts/memory.ts:65`), which the harness invokes **only** in its `persistCompletedTurn === undefined` fallback branch (`harness.ts:1660-1679`) — and `BujoMemoryStore` **always** defines `persistCompletedTurn` (`store.ts:449`). So for the shipped bujo host, the entire queue built by `initializeCaptureQueue()` (`store.ts:331`) — `captureQueue`→`captureAccepted`→`captureTurn`→`extractCapturePlan`→`distill`/`extractEntities` (items 8-9 above) — is one dead-in-shipped-product subsystem, reachable only through the public `scheduleCapture`/`.capture()` surface a third-party `MemoryStore` embedder could still call. | **Not a blind delete** — decide `scheduleCapture`'s fate on `BujoMemoryStore` first: either (a) keep the queue as the documented legacy fallback path for third-party `MemoryStore` implementers who don't implement `persistCompletedTurn`, with an explicit "unused by the shipped bujo host" doc comment, or (b) delete the whole subsystem from `BujoMemoryStore` and give it a thin no-op/throwing `scheduleCapture` instead. **Acceptance:** whichever is chosen, a doc comment on `scheduleCapture` states which path the shipped host takes, and (if deleting) the queue-building code and its two feeder functions are gone with tests still green. |
| 12 | `store.ts:666-687` — `BujoMemoryStore.reflect()` (method, distinct from the module-level `reflect.ts` function, which stays — see §B.1) | B2 · V3 | `grep -rn "\.reflect("` (excl. `dist/`, `.test.ts`) → 0 hits anywhere in `packages/agent-app/src`; not on the `MemoryStore` contract. CLI uses the standalone `reflect()` function directly (`cli.ts:100`), not this method. Live instances: 0 hits for `.reflect(`. | Delete. **Acceptance:** grep 0; CLI `reflect` subcommand behavior unchanged (it never called this method). |
| 13 | `store.ts:816-823` — `BujoMemoryStore.decay()` | B2 · V3 | Same pattern as #12: `grep -rn "\.decay("` → 0 hits outside tests; no `memory-bujo decay` CLI subcommand exists. | Delete. **Acceptance:** grep 0. |
| 14 | `search/vector-index.ts` (240 LOC) + `search/chunks.ts` (91 LOC) — `VectorMemoryIndex`, `createVectorMemoryIndex`, `gatherMemoryChunks`, `EntityLike` | B3 · V3 | `grep -rln "VectorMemoryIndex\|gatherMemoryChunks\|createVectorMemoryIndex\|MemoryChunk\b"` (excl. `packages/memory/`, `dist/`) → empty; only self re-exports (`search/index.ts:13-14`). Superseded entirely by the SQLite+sqlite-vec path in `store/db.ts`. `README.md:198` still advertises it as current API — doc/code drift. Live instances: 0. | Delete both files, and remove the `README.md:198` advertisement. **Acceptance:** grep 0 outside `dist/`; README no longer lists it as current public API. |
| 15 | `store/types.ts:52` + `schema.ts:33` CHECK constraint + `db.ts:938,1294,1313` — `"about"` `MemoryEdgeKind` value | B2 + B3 · V3 | `grep -rn "'about'\|\"about\""` in `packages/memory/src` filtered to insert/values/push/write → **no writer anywhere**, only type unions + read-side tolerance + the CHECK constraint. `db.ts:1277`'s own comment already calls it "retired" while `rebuild.ts:90-91` contradictorily says "deferred to P3+" — same fact, opposite framing. | Cheap fix: correct the comment drift to "retired" and add a one-line "reserved/unused" doc note (no migration needed). Higher-effort fix: actually retire the value from the type union + CHECK constraint (requires a schema migration — treat as a v2-scope item, not a v1 blind delete). **Acceptance (cheap path):** both comments agree; **acceptance (migration path):** migration + regression test pass and no code path can construct an `"about"` edge. |

### A.3 — Repo: dead exports, `packages/runtime-adapter`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 16 | `runtime-helpers.ts` — `assertBaseRunOptions`, `readLastStringUserMessage`, `buildRuntimeResult`, `applyTemporaryEnv`, `withTemporaryEnv` | C2 · V4 | `for sym in <5 names>; do grep -rn "$sym" packages extras demos scripts website docs; done` (excl. `runtime-helpers.ts`/`.test.ts`, `dist/`, `.d.ts`) → every symbol resolves to a single hit in `runtime-adapter/src/index.ts`'s barrel re-export only. 0 hits in both live instances. Structurally unreachable by the agent-runtime bridges these were built to dedupe (importing runtime-adapter from agent-runtime would violate the zero-workspace-dependency package-boundary rule). | Delete; if a future real dedup effort needs this, re-extract from the actual duplicated call sites when one exists. **Acceptance:** grep 0 across repo + both live instances. |
| 17 | `runtime-adapter.ts` — `acceptedSdkIdsForBackend`, `listMonoRuntimeSelectionTable` | C2 · V4 | Same grep sweep as #16 → both resolve to a single hit in `index.ts`'s barrel re-export only. Explicitly documented as "NOT wired into agent-host routing" with no current consumer anywhere in `agent-app`/`agent-harness`. | Delete unless Robert has a concrete near-term consumer planned (the doc comment implies forward-looking intent — if so, keep and cite the consumer in the doc comment instead). **Acceptance:** grep 0, or a real consumer is added and cited. |

### A.4 — Repo: dead exports, `tui` / `session-web` / channel adapters / extras

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 18 | `packages/tui/src/agent/responder.ts` — `TuiAgentCancelledError`, `isTuiAgentCancelledError` | C4 · V5 | `grep -rn "TuiAgentCancelledError\|isTuiAgentCancelledError" --include=*.ts` (excl. node_modules/dist) → definition, `index.ts` re-exports, and an unrelated **independently-declared** same-named class in `agent-contracts/__tests__/contracts.test.ts:78` (not an import of this one). `chat.ts` uses the base `agent-contracts` `isAgentResponseCancelledError` directly instead. | Remove the wrapper class + re-export, or document a real external consumer first. **Acceptance:** grep shows only the unrelated test-file declaration remaining (or 0, if that's cleaned up too). |
| 19 | `packages/tui/src/runtime/version.ts` — `TUI_PACKAGE_VERSION` | C4 · V5 | Hardcoded stale `"0.1.0"` vs actual package version `0.11.2`; grep shows only definition + `index.ts:75` re-export, never read anywhere (no `--version` flag, no help-overlay use). | Wire to `package.json` at build time and surface via `--version`/help overlay, or delete. **Acceptance:** `--version` prints the real version, or the symbol is gone. |
| 20 | `packages/session-web/src/history.ts:105` — `listInstanceSessions` | C4 · V5 | `grep -rn "listInstanceSessions\b"` → definition + `history.test.ts` only; absent from `index.ts`. "Back-compat alias" with no production caller (aggregator uses the summary/page functions directly). | Delete, or re-verify the back-compat rationale against a real external consumer before keeping. **Acceptance:** grep 0 (excl. its own test, removed alongside it). |
| 21 | `packages/telegram-adapter` — `showThoughts` (threaded through `adapter.ts:190`, `bot.ts:1255-1256`, `message-stream.ts:49`) | C5 · V5 | `grep -rn showThoughts packages --include=*.ts` (excl. tests/dist) → 5 hits: 2 type declarations, 1 forward in `bot.ts`, 1 hardcoded `true` set at `channels.ts:1440`; **zero** read site — `TelegramMessageStream`'s constructor (`message-stream.ts:252-307`, read in full) never consumes `options.showThoughts`. | Remove the field from `TelegramAdapterStreamOptions`/`CreateTelegramBotOptions.stream`/`TelegramMessageStreamOptions`, and drop the hardcoded `showThoughts: true` at `agent-app/src/channels.ts:1440`. **Acceptance:** grep 0 for the field name; existing telegram behavior unchanged (it was already a no-op). |
| 22 | `packages/slack-adapter/src/config.ts:432-449` — `redactSlackAdapterConfig` + `index.ts:99-104` export | C5 · V5 | `grep -rln redactSlackAdapterConfig` outside `slack-adapter/` → nothing. Production `agent-app` uses the generic field-spec redaction path instead; the parallel Telegram helper (`redactTelegramAdapterConfig`) **does** have a real consumer (`demos/final-agent`), so this is a genuine asymmetry, not a false-positive pattern. | Delete it, or wire `agent-app`'s demo/diagnostic surfaces to actually call it (matching what the Telegram sibling gets). **Acceptance:** grep 0 outside `slack-adapter/`, or a real consumer is added. |
| 23 | `extras/a2a-adapter/src/idempotency.ts:1129` — `cloneRecord` | D1 · V6 | `grep -rn "cloneRecord\b" --include=*.ts,*.md,*.mjs .` (excl. node_modules/dist, and excl. the unrelated same-named `cloneRecord`/`cloneRecords` in `packages/agent-app/src/continuation-store.ts` — a different package) → only the definition. Not exported from `index.ts`. Live instances: 0. | Delete. **Acceptance:** grep 0 (correctly distinguishing from the unrelated agent-app symbol of the same name). |

### A.5 — Repo: dead exports, `packages/config`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 24 | `packages/config/src/config.ts:99-102` — `DEFAULT_ARTIFACT_RETENTION_MAX_AGE_DAYS`, `DEFAULT_ARTIFACT_RETENTION_MAX_COUNT`, `DEFAULT_MEMORY_ARTIFACT_RETENTION_MAX_AGE_DAYS`, `DEFAULT_MEMORY_ARTIFACT_RETENTION_MAX_COUNT` | C7 · V5 | `grep -rln` the 4 names across `packages extras demos scripts website docs` (excl. node_modules/dist) → only `config/src/index.ts` (re-export) + `config/src/config.ts` (definition + real internal use at `:465-500`). No importer anywhere else — dead **as public exports only**, not internally. | Stop re-exporting from `index.ts` (keep package-private), or wire them into `doctor`/config-reference output (their apparent intended use). **Acceptance:** grep confirms no external importer either way; internal use at `:465-500` unaffected. |

### A.6 — Repo: orphan scripts, dead fixtures, untracked build output

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 25 | `scripts/memory-capture-baseline-probe.mjs` (133 lines) + `scripts/fixtures/memory-capture-mixed.baseline.json` + `scripts/fixtures/memory-capture-mixed.json` | E2 · V7 | One-off baseline probe from PR #202, hardcoded `EXPECTED_HEAD = "54f3fb49…"` — throws immediately on any other HEAD (repo is 10+ commits past it, so it cannot currently run at all). `grep -rn memory-capture-baseline-probe . --include=*.{json,yml,yaml,mjs,md,js,ts}` (excl. the file itself) → **no matches**: not referenced by any `package.json` script, workflow step, or test. `git log --oneline -- scripts/memory-capture-baseline-probe.mjs` → single commit (`e15ad34f`). | Delete the script and its two paired fixtures, or move under a `docs/history/`-style archive if the historical evidence value is worth keeping outside the working tree. **Acceptance:** files gone (or archived); grep for the script name (excl. this ledger) returns 0; no `package.json`/workflow reference breaks. |
| 26 | `packages/mono-agent/` (dist + `node_modules` only, untracked) | E3 · V7 | `git ls-files packages/mono-agent` → empty; no `package.json`; `git log --oneline --all -- packages/mono-agent` shows only the #174/#175 rename commits. Leftover build output from the pre-rename `mono-agent` package, superseded by `packages/create-mono-agent`. | Local housekeeping only — `rm -rf packages/mono-agent`; nothing to commit. **Acceptance:** directory gone; `pnpm-workspace.yaml`'s `packages/*` glob unaffected (no manifest ever existed there). |
| 27 | `packages/memory-supermemory/` (dist + `node_modules` only, untracked) | E3 · V7 | `git ls-files packages/memory-supermemory` → empty; no `package.json`; dist mtime predates the #198 move to `extras/memory-supermemory/` (which **is** git-tracked and live). | Local housekeeping only — `rm -rf packages/memory-supermemory`. **Acceptance:** directory gone; `extras/memory-supermemory/` (the real package) unaffected. |
| 28 | `.claude/worktrees/` (empty directory) | E3 · V7 | `ls -la .claude/worktrees` → empty; `git worktree list` shows 0 worktrees under it; `git check-ignore -v .claude/worktrees` resolves to the *personal* `.git/info/exclude:11`, not the committed `.gitignore` — so this is a stale first-draft convention superseded by `~/.config/superpowers/worktrees/mono-agent/` (all 44-50 live worktrees on the box are rooted there instead). | Remove the directory, and fix the stale path references in `agents/implementer.md:21` and the #119 epic body item 9 (both still say `.claude/worktrees/<name>`). **Acceptance:** directory gone; `agents/implementer.md` and the #119 body cite the actual convention. |

### A.7 — Live instance: `~/personal-agent`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 29 | `bin/heartbeat-prefilter` (binary) + `bin/lib/heartbeat-prefilter.mjs`'s `runHeartbeatPrefilter`/`shouldRunHeartbeat`, plus `launchd/com.user.personal-agent-heartbeat.plist` — **AMENDED/narrowed scope (V9)** | F1 · V9 | `grep -rn heartbeat-prefilter ~/personal-agent` (no include filter, excl. node_modules/.git) → referenced only by the dead plist, `tests/heartbeat.test.mjs:31`, and the intentional resurrection guard `tests/docs-contract.test.mjs:140`. `launchctl list \| grep personal-agent-heartbeat` → not loaded. **V9 narrows the original claim**: of the file's four re-exports (`readHeartbeatState`, `writeHeartbeatState`, `defaultHeartbeatState`, `HEARTBEAT_MAX_FUTURE_SKEW_MS`), only `readHeartbeatState` is actually consumed (by `bin/agent-watchdog:10`) — and even that consumer could import it straight from `heartbeat-state.mjs` instead. `writeHeartbeatState`/`defaultHeartbeatState`/`HEARTBEAT_MAX_FUTURE_SKEW_MS` are themselves **already unused** here: `heartbeat.mjs` imports its own copies directly from `heartbeat-state.mjs`, bypassing this file entirely. So cleanup is *easier* than the raw finding stated. | Delete `bin/heartbeat-prefilter` binary + `runHeartbeatPrefilter`/`shouldRunHeartbeat` + the 3 truly-unused re-exports; repoint `agent-watchdog`'s one live import of `readHeartbeatState` to `heartbeat-state.mjs` directly; delete the dead plist (the installer only ever removes it by filename via `RETIRED_PLISTS`, never stages its content); delete the now-pointless `shouldRunHeartbeat` test cases. **Acceptance (AMENDED per V9 NEW-2 — the original acceptance check was unsatisfiable):** `grep -r heartbeat-prefilter bin/` returns no matches, **excluding** the intentional resurrection guard at `tests/docs-contract.test.mjs:140`, which must keep asserting non-resurrection. |
| 30 | `~/personal-agent/--limit/` (empty, 0-row wacli SQLite store) | F1 · V9 | Structurally valid but empty store created at the agent's own working directory (`cwd: agentDir`, `focus-scan-snapshot.mjs:367`) from a past `wacli ... --limit N` CLI-argument mishap. Already gitignored (`.gitignore:18`) but never deleted; its `.db-shm` file was still touched as recently as the audit day. The real store (`~/.wacli/wacli.db`, 13MB, actively synced) is unaffected. Third-party `wacli` artifact, not framework code. | Delete the directory. **Acceptance:** directory gone; `~/.wacli/wacli.db` continues to serve wacli normally (no observable behavior change). |

### A.8 — Live instance: `~/a8c-agents`

| # | Item | Territory | Proof | Removal action + acceptance check |
|---|---|---|---|---|
| 31 | `activity-digest/`, `p2-watcher/`, `slack-sweep/` (~1.5 GB combined `node_modules` + independent lockfiles) — **AMENDED disposition (V9)** | F2 · V9 | Not started by any current launchd label (`launchctl list \| grep a8c` shows none loaded); launchd manifests carry the `.retired-a8c-fleet` suffix; excluded from the active `pnpm-workspace.yaml` since the 2026-07-13 migration. **V9 amends "just `rm -rf` it"**: `git ls-files activity-digest/` → **11 tracked files** (only `node_modules` is gitignored), and all three dirs are referenced **by name** as `legacyAgents`/`LEGACY` in `validate-fleet.mjs:16,612`, `v2-migration-snapshot.mjs:151`, and `bin/agents:64`. Disposal is not a bare directory deletion. | Set and record an explicit soak-period end date (e.g. 30 days from the 2026-07-13 cutover) in `docs/migration.md`, then `git rm` the three directories (not a bare `rm -rf`) **and** retire or repoint the `legacyAgents` guard references in the three scripts above (or point them at the external migration-snapshot store per `docs/migration.md:3-9`, which is the authoritative rollback artifact anyway). **Acceptance:** directories gone from `git ls-files`; `validate-fleet.mjs`/`v2-migration-snapshot.mjs`/`bin/agents` no longer dangle-reference them; ~1.5GB reclaimed on disk. |
| 32 | `legacy/` (empty, 0 entries) | F2 · V9 | `ls -la ~/a8c-agents/legacy` → 0 entries, 0 bytes. Unreferenced by any script; the only doc language mentioning "retired directories" actually points at the three dirs in #31, not this one. | Delete the directory, or add a one-line `README.md` recording what it was reserved for if there's a reason to keep the placeholder. **Acceptance:** directory removed or documented; no script references it either way (already true). |
| 33 | `orchestrator/mono-agent.config.json.bak-pre-0.6.0` (untracked) | F2 · V9 | `git check-ignore -v` → matched by `.gitignore:22 *.bak-*` (untracked); mtime 2026-07-08; superseded by the current `mono-agent.config.json`; not read by any script in scope. | Move to the external migration-snapshot store, or delete once the 0.6.0-era rollback window is judged closed. **Acceptance:** file gone or moved; no script reads it (already true today). |
| 34 | Brain Core's legacy `/mcp/agent` endpoint | F2 · V9 | `grep /mcp/agent` across all 5 worker configs → **0 hits** — confirmed no active worker references it. Per `docs/architecture.md:177-179` / `docs/runbook.md:34`: "remains available only for compatibility and is not wired to an active worker." **Caveat (V9): the endpoint's actual implementation inside `packages/brain-core/src/server.ts` was not independently opened** — this check was boundary-level only (config-side), so confirm in-source before deleting (see §C). | Schedule removal in the next Brain Core minor version, **after** an in-source check of `packages/brain-core/src/server.ts` confirms no residual internal reference. **Acceptance:** in-source check done; endpoint removed; all 5 worker configs and their tests still pass. |

---

## B. Deprecation & legacy classification (E4 ledger, verifier-adjusted)

Source: the dedicated security/deprecation sweep (`_raw/E4-security-deprecation-sweep.md`), verified by `V8`, supplemented with legacy/back-compat items that fell outside E4's exact `legacy|back-compat|backward` grep scope but were independently classified by other territory audits and their verifiers.

### B.1 — The 4 canonical `@deprecated`-tagged markers (verifier found a 4th E4's own table omitted)

| Marker | File:line | Classification (verifier-adjusted) | Keep-until condition |
|---|---|---|---|
| `reflect()` (module-level function) | `packages/memory/src/bujo/reflect.ts:24` | **Load-bearing-compat.** Still invoked by the shipped `memory-bujo reflect` CLI subcommand (`cli.ts:99-113`), documented as an intentional legacy compat surface across 5 doc pages. This is the CLI-reachable surface — distinct from the store-method wrappers below. | Published npm CLI surface (`@mono-agent/memory` ≥0.3.0); keep until a deliberate v2 breaking-change removal, not a v1-freeze deletion. |
| `BujoMemoryStore.reflect()` (method) | `packages/memory/src/bujo/store.ts:666` | **AMENDED by V8/V3 → idle public-API no-op**, not "reachable via the CLI" as the raw E4 table implied — the shipped CLI calls the *free function* above, not this method. Zero in-repo callers (grep-confirmed). Listed in §A.2 #12 as removable-now internally. | If kept at all (published-API stability for a hypothetical direct-import consumer), flag as a v2-removal-audit candidate — not load-bearing for any known caller. |
| `BujoMemoryStore.decay()` (method) — **4th marker, missing from E4's own §4a table (V8 NEW-3)** | `packages/memory/src/bujo/store.ts:816` | Same as above: **idle public-API no-op**, zero callers. Listed in §A.2 #13 as removable-now internally. | Same as above. |
| `MemoryDb.applyDecay()` | `packages/memory/src/store/db.ts:1980-1988` | **Idle public-API no-op**, documented compatibility shim ("Salience is a static canonical Bullet field in v1… kept as a compatibility no-op"). Zero in-repo callers; two tests `vi.spyOn` it specifically to assert it is *never* called by current reflect/consolidate pipelines. | **Load-bearing only as external-package API compatibility** — any older direct consumer of `@mono-agent/memory/store` calling `applyDecay` keeps getting `{decayed: 0}` instead of a `TypeError`. Keep through v1; real removal candidate only once external-compat is no longer promised (a future major bump). |

### B.2 — The 61-file `legacy|back-compat|backward` code ledger (E4 §4b, verifier-confirmed intact)

**Result, verifier-confirmed: 0 removable-now, 57 load-bearing-compat, 4 naming-only-false-positive.** No file in this sweep is dead weight. V8 spot-checked the strongest load-bearing claims (`LEGACY_TOOL_ALIASES` consumption, `repairLegacySalience`'s in-path call, the continuation `legacy_missing` state-machine member) and found no misclassified-dead entry.

**Load-bearing-compat (57), grouped by keep-until condition:**

| Keep-until condition | Items |
|---|---|
| **Config/CLI dual-field back-compat — removable only via a breaking config change, no date set** | `runtime.fallbackModels` / `MONO_AGENT_FALLBACK_MODELS` / `--fallback-models` CSV (mutual-exclusion guarded in `cli.ts:699`, `config.ts:323`, `layered-loader.ts:642`, `types.ts:183`, `config-view.ts:165`, `config-reference.ts:1076`, `readiness-probe.ts:482`, `runtime-routes.ts:6`, `configured-agent.ts:323`, `wizard/answers.ts:286`, `wizard/from-flags.ts:50`) — 10 files, one dual-API concept |
| **Snake_case tool-name rename aliases — keep while any existing `allowedTools`/`disallowedTools` config could reference the old names** | `LEGACY_TOOL_ALIASES` (`adapter-send-tools.ts:1478`, `modules/known-tools.ts:46`, `modules/index.ts:25` re-export) and `RUN_HISTORY_LEGACY_TOOL_NAME` (`run-history.ts:23`) |
| **Continuation-store v1→v2→v3 migration — keep until fleet telemetry confirms no v1/v2 stores remain** | `continuation-store.ts:290,299-351` (v1 monolith idempotent migration + `records-v2`/`continuations-v1.json` retained by design for audit/rollback), `continuations.ts:179` (`legacy_missing` state), `doctor.ts:2220` |
| **Init/wizard/onboarding shape tolerance — low-risk, no removal pressure** | `init.ts:60`, `wizard/answers.ts:286` |
| **Memory-package migration/back-compat internals — keep, actively exercised by real rebuild/replay operations** | `memory-command.ts:280` (adopt-replay CLI wiring), `memory-retrieval.ts:87` (structural capability probe for stores without `persistCompletedTurn` — confirmed live for the external Supermemory backend), `bujo/audit.ts:636`, `bujo/capture-outbox.ts:686` (`repairLegacySalience`, in-path on every capture replay), `bujo/cli.ts:99`, `bujo/explicit-forget.ts:602`, `bujo/generations.ts:156`, `bujo/grammar.ts:43` (`LEGACY_LINE_RE`, parses the older `<!--mem …-->` bullet format), `bujo/graph.ts:376` (`isLegacyHostObservation`), `bujo/index.ts:22`, `bujo/json.ts:33`, `bujo/path-safety.ts:154`, `bujo/rebuild.ts:1180` (auditor calls this "the single most important piece of back-compat in the repo" — protects prior journal data during rebuild), `bujo/reconcile.ts:249` (`planLegacyAction` — live core logic, not deprecated), `bujo/replay-adoption.ts:96`, `bujo/replay-projection.ts:290`, `bujo/store.ts:308`, `store/db.ts:182`, `store/types.ts:84` |
| **Agent-harness/agent-contracts plugin seams — keep for third-party `MemoryStore`/`ConversationHistoryStore` implementers** | `durable-history.ts:16,36` (`LEGACY_STORE_VERSION`, `LEGACY_CONVERSATION_LOCK_PATTERN`), `harness.ts:1674` (legacy-store rapid-log fallback), `agent-contracts/coded-error.ts:28`, `agent-contracts/index.ts:168`, `agent-contracts/memory.ts:60` (`appendHostSummary`/`scheduleCapture` legacy pair — see §A.2 #11's capture-queue nuance), `agent-harness/types.ts:337` (`skillDisclosure: "full"` — this is the current *default*, not a phase-out) |
| **Config-package fallback plumbing — same concept as row 1, package-internal** | `config-view.ts:165`, `config.ts:323`, `layered-loader.ts:642`, `types.ts:183` |
| **Cron/webhook single-endpoint/single-job on-ramp — primary onramp for simple configs, actively tested, no removal pressure** | `cron-adapter/scheduler.ts:298` (default `overlap:"skip"`), `webhook-adapter/config.ts:45`, `webhook-adapter/server.ts:141` |
| **Session-web / TUI published-API back-compat — cheap to keep, unknown external consumer set** | `session-web/history.ts:102` (list-rows alias), `session-web/session-model.ts:39` (`timezone`/`timeZone` mirror), `tui/data/replay.ts:57` (`listReplayRuns` bare-number overload) |
| **Observability back-compat read path — needed to not break existing Phoenix/trace history** | `observability/session-mapping.ts:283` (`refineRunSource`, "robust to legacy summaries") |
| **Runtime-adapter migration bridge — `resolveRuntimePolicies` is the supported bridge OFF legacy, not itself legacy** | `runtime-adapter.ts:434`, `runtime-policies.ts:6`, `types.ts:145`. The legacy `options.settings` flat-bag *acceptance itself* is a kernel-side (agent-runtime) decision outside this package's scope; MIGRATION.md already documents "one deprecation warning per run" as the intended UX. |

**Naming-only-false-positives (4) — confirmed conservative, no removal risk either way:**

| File:line | Why it's not actually legacy |
|---|---|
| `agent-app/channels.ts:1058` | Describes the current, still-active `overlap:"skip"` cron default — nothing to migrate away from. |
| `cron-adapter/scheduler.ts:298` (also appears above under a real load-bearing row — see V8's note that #34 is a `naming-only` framing of the *same* line the "load-bearing" row also cites) | "Legacy" here is a label for the historically-established, still-current default, not a deprecated path. |
| `memory/bujo/reconcile.ts:249` | `planLegacyAction` is live, actively-called-today core per-candidate logic; "legacy" distinguishes it from the newer `reconcileBatch` planner, not a removable surface. |
| `tui/ui/components/event-list.ts:309` | `backward` is a directional array-index variable in visible-range math — unrelated to backward compatibility. |

### B.3 — Deprecation/legacy items outside E4's exact code-grep scope (CLI flags, docs layer, live-fleet aliases)

| Item | Territory | Classification | Keep-until condition |
|---|---|---|---|
| `--recipe <id>` flag on `init`/`validate`, `mono-agent recipes` command alias | A1, E1, E3, G1 · V1/V6/V7/V10 | **Removable, but undated.** Fully superseded by `--preset`/`mono-agent presets`; every call site prints a deprecation hint (`cli.ts:3420,3759`); full forward-mapping table in `docs/reference/recipes.md:114-123`. | No stated milestone — recommend scheduling a post-v1 minor removal once the config-consolidation-era scripts/docs are confirmed migrated. |
| `mono-agent doctor` / `mono-agent setup` aliases | A1 · V1 | **Not legacy** — no deprecation warning printed; treated as permanent friendly aliases. Included only to distinguish from the `--recipe` row above. | N/A — no action needed. |
| SRT install-lock schema v1 (`LegacyInstallLockRecord`, `"legacy"`/`"legacy-publishing"` lock kinds, `readSecureLegacyInstallLock`) | A3 · V1 | **Transitional/load-bearing.** Every *newly written* lock is schema v2; this branch only fires for installs predating the current release. | Keep until release notes/telemetry confirm no supported install base still holds a v1-schema lock. |
| `isProcessAlive` legacy owner-record fallback (`background.ts:1359-1364,1459-1470`) | A3 · V1 | **Low-risk legacy**, open-ended — unlike the SRT lock, no schema-version gate exists, so it is silently permanent unless removed deliberately. | No gate today; recommend adding a sunset condition (schema-version check) rather than leaving it open-ended forever. |
| `RETIRED_MEMORY_ENV_KEYS` (7 pre-v2 memory env vars) | C7 · V5 | **Load-bearing (for now).** Tolerated + warned, never thrown — prevents a stale pre-Memory-v2 config from silently doing nothing. | Safe to delete post-v1 once the fleet and any external consumers are confirmed off these keys. |
| `memory.reflection.*` / `memory.migration.*` JSON blocks | C7, E1 · V5/V6 | **Load-bearing (for now).** Kept typed solely so old JSON files don't hard-fail; actively ignored at runtime, flagged via `findRemovedConfigWarnings`. | Removing would break startup for stale environments — worse than a warning. No removal date. |
| `"ultra"` in `EFFORT_LEVELS` | C7, G1 · V5/V10 | **Load-bearing but under-documented.** Kept so an explicitly-configured `ultra` isn't clobbered by keyword escalation, but yields the *same* low-thinking behavior as `minimal`/`low` at the runtime layer (`turn-runner.js` falls through to `"low"` for `ultra`) — a real honest-ops doc gap, not a removal candidate. | Not removable — needs a README caveat (not a "keep until"; this is a documentation fix, not a sunset). |
| `@mono-agent/memory-mcp` package (fully retired) | E1 · V6 | **N/A, already removed.** Docs (`docs/memory/capture-and-recall.md:394-397`, `docs/memory/index.md`) give accurate 3-path migration guidance. | Done — no further action. |
| `@mono-agent/operator-console` (retired per PR #28) | E1 · V6 | **N/A, already removed, zero stale doc references found.** | Done — no further action. |
| A8C fleet: `A8C_GWS_*` env aliases for `A8C_ACTION_*` | F2 · V9 | **Load-bearing, one-release compatibility.** Actively validated for consistency (fails closed on alias/canonical mismatch). | Removable on the next release once operators have fully migrated to `A8C_ACTION_*`. |
| A8C fleet: `a8c_gws_stage_action` tool alias / Slack `a8c_gws_*` button IDs | F2 · V9 | **Load-bearing, same one-release pattern** — legacy Slack messages posted before the rename may still carry the old action IDs. | Removable once no unresolved legacy approval cards remain outstanding (unverified whether any currently do — see §C). |
| A8C fleet: V1 broker delegation/result records + `a8c-control-broker migrate-v2` tooling | F2 · V9 | **Load-bearing.** The v1 schema itself is fully retired; the *migration command* remains the only sanctioned recovery path. | Do not remove until an audit confirms zero v1-shaped records remain in `data/control-broker/`. |
| A8C fleet: `.retired-a8c-fleet`-suffixed launchd manifests / `legacy:stop` (no `legacy:start`) | F2 · V9 | **Deliberately load-bearing — audit trail only.** The three retired workers depended on a webhook receiver that no longer exists. | Keep indefinitely; removing would delete the historical evidence chain the migration docs rely on. |
| `~/personal-agent` webhook path reservations: `webhook/health-metrics.md`, `webhook/heartbeat-evaluate.md` (`enabled: false`) | F1 · V9 | **Load-bearing, not removable.** Deliberate "path reservations" (`AGENTS.md:117-125`) preventing the model-backed webhook adapter from claiming those paths. | Keep — these are intentional reservations, not stale artifacts. |
| `~/personal-agent`: `.mono-agent/heartbeat/state.v2-pre-cutover.json` | F1 · V9 | **Load-bearing rollback artifact.** | Do not prune without an explicit policy decision. |
| `~/personal-agent`: `.mono-agent/memory/legacy/` (pre-v3-rebuild daily notes) | F1 · V9 | **Load-bearing audit trail** for the 2026-07-14 memory rebuild (`sourceFingerprint`/`policyVersion` in the manifest). | Safe to prune only under an explicit retention policy, not casually. |

---

## C. Suspected, unproven

Items where a real question was raised but the verifier could not (or did not) close the loop — listed so a future pass knows exactly what evidence is still missing, rather than either asserting or dismissing the claim.

| Item | Territory | What's claimed | What proof is missing |
|---|---|---|---|
| `notifyFallbackConversationId` start-time snapshot drift | A4 · V2 | Webhook/cron `inferUniqueNotifyDestination` runs once inside `start()` and is baked into `notifyFallbackConversationId`, while delivery re-resolves per run — a plausible destination-drift bug. | The mechanical snapshot-vs-resolve-per-run claim is confirmed, but the downstream `replyTo` consequence lives in `agent-harness`/adapter code neither the auditor nor V2 fully traced. Stays UNPROVEN exactly as the auditor hedged. |
| `rebuildFromMarkdown` (`packages/memory/src/bujo/rebuild.ts:93`) external-consumer status | B2 · V3 | Zero *internal* callers proven (product path is `safeRebuildMemoryIndex`); disposition recommended is "keep as documented public utility," not delete. | Whether any external npm consumer of `@mono-agent/memory` actually calls this low-level function directly is unknown — only internal reachability was checked. |
| `createInMemoryHistoryStore`/`InMemoryConversationHistoryStore` (`agent-harness/src/history.ts`) external-consumer status | C1 · V4 | Zero production callers in-repo; plausible minimal reference implementation for a third-party embedder who wants non-durable history. | Whether any real external embedder of `@mono-agent/agent-harness` actually uses this is unverified — "no shipped host uses it" was confirmed, "no one uses it" was not. |
| `~/.pi/personal-agent/settings.json`: `defaultModel: "qwen3.6"`, `defaultProvider: "ollama"` | F1 · V9 | Vestigial `pi` CLI init defaults, presumed harmless since actual routing goes through `mono-agent.config.json`. | Not independently re-verified by V9 this pass — carried forward as UNPROVEN, not re-confirmed. |
| `.mono-agent/great-triage-2026-07-11.applied.log` / `.before.tsv` | F1 · V9 | One-off bulk-cleanup operation logs sitting loose at `.mono-agent/` root; presumed harmless clutter. | Not re-verified by V9 this pass; whether the operation is durable enough to archive/delete the logs was not checked. |
| Historical cron overlap/skip tallies (e.g. focus-scan-hourly 59/18) | F1 · V9 | Auditor's original framing implied a real historical problem now resolved (22/1 recent). | **Downgraded to PLAUSIBLE by V9**: the pre-2026-07-10 counts live in a rotated log, and the current `out.log` entries are giant per-run JSON blobs that defeat cheap `grep -c` — the exact historical tallies were not independently reproduced. |
| MCP SDK's documented pattern for `res.on("close")` registration order | D1 · V6 | Auditor claims the SDK's documented pattern registers the close listener before `handleRequest`, and `extras/agent-orchestrator/src/index.ts:113-119` deviates from it. | The in-repo deviation itself is confirmed; the "SDK's documented pattern" claim was **not** independently verified against MCP SDK source by V6 — accepted as plausible, not proven. |
| OpenAI SSE tool-call rendering has no size cap (`openai-api-adapter/server.ts:1069-1097`) | C6 · V5 | Asymmetry claim: TUI's `truncateEvent` bounds NDJSON frames; openai's `openWebUIToolDetails` allegedly writes raw args/result to SSE with no cap. | V5 marked this **PLAUSIBLE, not CONFIRMED** — the exact lines were not read/line-verified in that pass. |
| A8C fleet: `a8c_gws_stage_action` tool alias / Slack `a8c_gws_*` button IDs — whether any outstanding legacy approval cards still exist | F2 · V9 | Keep-until condition in §B.3 is "no unresolved legacy approval cards remain outstanding." | Doc-based only (`README.md:77,115`); not independently exercised against live Slack message/card state — unknown whether the condition is already met. |
| Brain Core `/mcp/agent` endpoint's actual removability | F2 · V9 | All 5 worker configs confirmed to have zero references (config-side / boundary check). | The endpoint's implementation inside `packages/brain-core/src/server.ts` was **not independently opened** — boundary-scope only. Confirm in-source before deleting (see §A.8 #34). |

---

## D. NOT dead — refuted claims

Preserved so nobody re-deletes these on a stale hunch from a future skim of the raw artifacts. Rows are of two kinds: claims proposed as dead/cruft by a raw auditor and **refuted** (or materially amended) by its verifier (~4), and adjacent candidates the verifiers **proactively re-confirmed as live** while running proof-of-death greps — kept together because both classes carry the same risk of a future false deletion.

| Claim (as originally raised) | Territory | Why it's refuted | Corrected understanding |
|---|---|---|---|
| `attestManagedBackgroundRuntime` (+ 3 associated types) is fully-implemented, never-called dead code | A3 · V1 | **REFUTED.** The auditor's proof grep was scoped only to `packages/agent-app/src` and missed the cross-package consumer. `scripts/managed-runtime-attestation-probe.mjs:71` imports and calls it; `scripts/fleet-green-check.mjs:1852-1861` spawns that probe for every managed-runtime instance as the documented `observability.fleet-loaded-code` attestation (`feature-registry.md:156`) and the `fleet-deploy` skill's daily 7-day tracker. | Live, load-bearing attestation infrastructure. Not exported from `index.ts`, but that's irrelevant — the probe imports the dist module directly. **Do not delete or "wire up"; it is already wired up.** |
| `ReadSettingsJsonResult.version` (sha-256 config version field) is dead weight, no caller reads it | C1 · V4 | **REFUTED.** The auditor grepped only direct `readSettingsJson` consumers and missed the wrapper. `packages/config/src/json-source.ts:220-230` `readMonoAgentConfigJson` wraps it and spreads `...result` (carrying `version`) into its own result type. | `packages/agent-app/src/local-configuration.ts` consumes it as a real optimistic-concurrency (CAS) token: `proposal.baseVersion !== current.version` (:611), `sha256(configBefore) !== current.version` (:620), `expectedConfigVersion: current.version` (:662), and the commit-verify guard at `:1446`. **It is load-bearing** — the concurrent-config-write race for the conversational proposal path is genuinely addressed by it. (Residual, minor, separately noted: the *direct* `writeSettingsJson` merge path used by channel-adapter setup/init has no CAS of its own — but that is a distinct, smaller observation, not a reason to touch the `version` field.) |
| `completedTurnInflight` (SupermemoryStore) grows unbounded alongside `completedTurns` | D1 · V6 | **AMENDED/refuted-in-part.** Only `completedTurns` (`store.ts:56`) actually grows unbounded. | `completedTurnInflight` (`store.ts:57`) **is** evicted — a `finally` block at `:155-157` deletes its entry after each turn settles, bounding it by concurrent in-flight turns, not lifetime. Any fix (§ D1 actionable steps) should name only `completedTurns`. |
| `scripts/lib/build-provenance.mjs` / `scripts/lib/memory-cleanup-calibration.mjs` are orphaned scripts-lib stragglers | E2 (flagged as a coverage-gap, not asserted dead) + G1 · V10 | **REFUTED, confirmed live by two independent passes.** `build-provenance.mjs` backs the root `build` script (`package.json:13`) and is imported by `build-with-provenance.mjs`, `build-provenance-probe.mjs`, `fleet-green-check.mjs`, `publish-release.mjs`, plus its own test. `memory-cleanup-calibration.mjs` is imported by `memory-benchmark.mjs`, wired to `package.json`'s `benchmark:memory` script. | Both are load-bearing tooling — `build-provenance.mjs` for build/release/fleet-deploy integrity, `memory-cleanup-calibration.mjs` for dev-invoked (not CI-gated) memory-quality calibration. Neither is orphaned; do not delete. |
| `records-v2/` legacy directory + `UPGRADED-TO-RECORDS-V3` guard file, created on every fresh install, is "a dead artifact created by live code" | A5 · V2 | **AMENDED/refuted as framed.** This is not simple dead-artifact fallout. | It is defensible defense-in-depth: proactively poisoning `records-v2/` on a brand-new store means a later rollback to v0.10 against this directory fails **closed** rather than reading a stale/absent snapshot. Keep as a P3 legibility nit at most (the fresh-install trigger condition could be narrowed); do not treat it as removable dead code. |
| `issueProgressCapability`/`releaseRun`/`enrichAssistantHistory` (agent-app `channels.ts` scope) look unreferenced from `app.ts`/`channels.ts` | A4 · V2 | **Confirmed correctly NOT dead** — the auditor avoided this false positive in the first place; V2 independently re-verified. | Consumed indirectly via harness typed interfaces (`turnHistoryEnricher`/`progressCapabilityIssuer` at `harness.ts:858,1616,1854-1856`), not directly from `app.ts` — a naive single-file grep would wrongly flag these as dead. |
| `TraceSourceHandle.heartbeat()` (observability) has no explicit external caller | C3 · V4 | **Confirmed NOT dead.** | Heartbeats fire via the internal `heartbeatMs` timer registered inside `registerTraceSource` — an intentionally-exposed escape hatch on a small typed handle, not unreachable code. |
| `handleTick` (cron-adapter `scheduler.ts:282`) looks internal-only/unused | C6 · V5 | **Confirmed NOT dead.** | Deliberately not re-exported from `index.ts`, but directly imported by `cron-adapter.test.ts:9` for defense-in-depth regression testing of the unrecognized-overlap-mode fallback. By design. |
| `CronJobConfig.notifyFailureCooldownHours` looks unused because it's absent from the projected runtime `CronJob` shape | C6 · V5 | **Confirmed NOT a bug.** | Intentionally **not** projected by `toCronJobs()` — `agent-app/src/channels.ts:1143` reads it directly off the retained `CronJobConfig[]` (not the projected runtime jobs) for host-side failure-notice cooldown logic. Confirmed live. |
| `RedactedWebhookAdapterConfig`/`RedactedCronAdapterConfig` (identity-transform types) look like no-op dead weight | C6 · V5 | **Confirmed NOT dead code.** | These configs hold no secrets — the identity transform is a documented, intentional consequence of applying a uniform `redact*Config` pattern polymorphically across every channel adapter. |
| `pi-sdk.js` deep-import path might still be reachable somewhere | C2 · V4 | **Confirmed already fully and correctly removed** — not a residual dead reference to chase. | Absent from the current `exports` map; `verify-deep-imports.test.mjs` carries an explicit negative assertion (`expect(specifiers).not.toContain(".../pi-sdk.js")`). No orphaned reference remains in `runtime-adapter/src/**`. |
| Cron `overlap:"queue"`/`"replace"` + `maxQueueDepth`/`overflow` machinery is dead/unused because the README says cron is skip-only | C6 · V5 (NEW-1) | **NOT dead code — it's a config-exposure gap, a different class of finding.** | The full overflow-policy state machine (`scheduler.ts:282-362`) is fully built and tested, and reachable by third-party programmatic callers of `startCronAdapter` — it's just unreachable from the **mono-agent product's own config surface** (`CronJobConfig` has no `overlap` field; `agent-app/src/channels.ts:1057-1063` hardcodes `overlap:"skip"`). This explains (and validates) why `docs/channels/cron.md` legitimately says "skipped, never queued" for the config-file audience. **Do not delete this machinery** — the fix (if any) is a README clarification that queue/replace are programmatic-only, not a code change. |
| `resources/srt/package.json` (`@mono-agent/managed-srt`, pinned `@anthropic-ai/sandbox-runtime@0.0.64`) might be stale/uncoordinated with the integrity-lock constant | G1 · V10 | **Confirmed correctly-maintained, not cruft.** | `MANAGED_SRT_VERSION`/`MANAGED_SRT_LOCK_SHA256` (`sandbox-manager.ts:45-47`) match the shipped `package.json`/`package-lock.json` exactly — V10 independently recomputed `shasum -a 256` on `package-lock.json` and it matched byte-for-byte. A correctly-maintained integrity-locked install input. |
| Root `vitest.config.mjs` / `website/pnpm-workspace.yaml` might be inconsistent or leftover | G1 · V10 | **Confirmed intentional, not an inconsistency.** | `vitest.config.mjs`'s exclude list matches the real gitignored worktree-copy directories (`.ultrawork/`, `.worklab-tmp/`); root `pnpm-workspace.yaml`'s `packages/*`+`extras/*` deliberately excludes `website/`, matching `website/pnpm-workspace.yaml`'s own stated isolated-Vercel-app rationale. Both match their in-file comments against actual repo layout. |
| a8c fleet's `activity-digest`/`p2-watcher`/`slack-sweep` (~1.5GB) are simple "dead weight, just `rm -rf`" | F2 · V9 (NEW-1) | **Amended, not simply refuted** — see §A.8 #31 for the full corrected disposition. | They are git-tracked (11 files each) and actively referenced by name as `legacyAgents`/`LEGACY` guards in 3 live scripts. Not bare directory deletion; also, the "something keeps them version-current" mtime inference in the raw finding over-reads what is likely just a routine git-checkout touch, not an active refresh daemon. |
