# V8 — adversarial verification of E4-security-deprecation-sweep

Verifier pass over `audit/_raw/E4-security-deprecation-sweep.md` at HEAD `5f27a0ec`.
Read-only everywhere except this file. Live instances inspected read-only; no secret values quoted.
gitleaks `8.30.1` (matches CI-pinned `v8.30.1`) available locally — F1 reproduced empirically with **synthetic** tokens in a throwaway scratch repo (never the real token).

---

## Part 2 — Findings (§2)

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| F1 (gitleaks Telegram-token gap) | **AMENDED** (evidence CONFIRMED + strengthened; severity lowered) | **P2** (was P1) | Reproduced exactly. Scratch `git init` repo + repo's own `.gitleaks.toml`, three `git add`ed files: (a) `token = <40 random>` → **FLAGGED** `generic-api-key`; (b) `token = <bare 35-char tail>` → **FLAGGED** `generic-api-key`; (c) JSON `{"telegram":{"botToken":"987654321:<35-char tail>"}}` → **MISSED** (`no leaks found` on that file). The embedded colon in the `digits:tail` shape defeats the `generic-api-key` capture — auditor's mechanism is correct. **Strengthened (see NEW-1):** I proved `gitleaks dir` does *not* respect `.gitignore` (a bare secret in a gitignored untracked file is still caught), so the untracked root `mono-agent.config.json` **is** scanned on every CI/local `check:secrets` run and silently passes — the "no leaks found" on the real repo is genuinely the regex gap, not a skipped file. **Severity P1→P2:** real defense-in-depth gap in the flagship channel's credential type, but no secret is committed, the primary control (gitignore + `tokenEnv` indirection, followed in all 9 live instances) is sound, and the gap only bites on a *future* `git add -f`/gitignore-loss mistake. Does NOT make "honest ops" false (repo never claims gitleaks covers all secret shapes). Bears on the "honest ops" premise as hardening — legitimately actionable (A1-1). |
| F2 (real 46-char Telegram token in untracked root config) | **CONFIRMED** (severity re-labeled) | **Informational / P3** (auditor's "P0-adjacent" is rhetorical scene-setting, not an actual severity) | All git facts verified: `git ls-files -- mono-agent.config.json` → empty (untracked); `git log --all --oneline -- mono-agent.config.json` → **zero** commits (never committed); `.gitignore:17` lists it; `git status --porcelain --ignored` → `!! mono-agent.config.json`. `telegram.botToken` length = **46**, matches `^[0-9]{6,10}:[A-Za-z0-9_-]{35}$` (value never printed). This is the maintainer's gitignored dev config working exactly as designed — not a leak, not a P0-class issue. Its only real function in this audit is to demonstrate F1's mechanism against a real token. Correctly not a freeze-blocker. |
| F3 (no dependency-vuln scanning in CI) | **CONFIRMED** | **P2** (justified) | `.github/dependabot.yml` absent (`ls` → No such file). No `pnpm audit`/`npm audit`/`dependency-review` anywhere (grep repo-wide, excl node_modules/lockfile → only self-references in the audit md). Read `scripts/check-oss-hygiene.mjs` in full: it is a **PII/consumer-fixture scanner** (non-synthetic fixture names + non-example `/Users|/home` paths), zero CVE logic — auditor's characterization is exact. P2 defensible for a 21-package published framework. |
| F4 (no npm `--provenance`; classic token not OIDC) | **AMENDED** (fact CONFIRMED; severity + novelty corrected) | **P3** (was P2) | Fact verified: `publish-release.mjs:302-310` builds `npm publish <tarball> --access <access> --tag <distTag>` + `npmConfigArgs()`, no `--provenance`; `npm-release.yml:75` uses `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` (classic token, not OIDC). **Three corrections:** (1) **Already tracked in OPEN issue #230** ("Release assurance: trusted npm publishing and isolated packed consumers") — E4 fails to cite it; A1-3 should reference #230, not file fresh. (2) **Sibling contradiction:** E2's F7 audits the identical gap, verifies it live (`npm view @mono-agent/agent-app@0.11.2` has no provenance field), and rates it **P3** as an honestly-disclosed, tracked limitation. (3) **"No cryptographic build attestation" is misleading** — the repo has a substantial *internal* build-provenance system (`assertBuildMarkerForHead`/`assertCurrentBuildProvenance` in `publish-release.mjs:133-159`: git-SHA marker tied to clean HEAD, `sourceState==="clean"` gate, frozen-tarball sha512 integrity match, output/dependency digests). What's absent is npm's public **sigstore** attestation badge — a hardening add, not "zero attestation." |
| F5 (`minimumReleaseAgeExclude` inert) | **CONFIRMED** | **P3** (justified) | Verified: `pnpm-workspace.yaml:13-24` lists 11 pinned excludes; no `.npmrc` anywhere (`find . -iname .npmrc -not -path "*/node_modules/*"` → empty); no `minimumReleaseAge` key set anywhere (grep repo-wide → only the `…Exclude:` line + the audit md). The base guard is off, so the exclude list gates nothing. "Looks-active-but-isn't" supply-chain hygiene, P3. |
| F6 (inconsistent live-instance `.env` perms) | **CONFIRMED** | **P3** (arguably P4; local-ops, not repo/DoD) | `stat -f "%OLp"` verified the exact split: `activity-digest`, `p2-watcher`, `slack-sweep` `.env` = **644**; `attention-radar`, `deep-research`, `delivery-copilot`, `knowledge-steward`, `orchestrator` + `~/personal-agent` = **600**. Same split mirrored on the paired `mono-agent.config.json` files (3×644 / 5×600); shared `~/a8c-agents/.{proactive,control,a2a,brain}.env` all 600. Accurate. Single-user Mac, so real-world exposure is low — P3 generous but fine. Note: this is a **live-instance ops fix**, outside the repo and any DoD clause. |
| F7 (recon "2 `as any`" doesn't reproduce) | **CONFIRMED** | n/a (correction, not a defect) | `grep -rnE '\bas any\b' packages/*/src --include="*.ts"` (excl `__tests__`) → **0** matches at `5f27a0ec`. Recon hint was stale; correctly flagged as a non-finding rather than asserted. |

---

## Part 3 — Dead code (§3)

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| §3 "None found" | **AMENDED** (conclusion stands; two nuances added) | n/a | Overall "no dead-weight to remove now" is **defensible**. But proof-of-life greps refine the deprecated-no-op story: `grep -rn "\.reflect(" packages` (excl tests/defs) → **0**; `grep -rn "applyDecay" packages extras demos scripts` → only the def + `.d.ts` + one comment; `grep -rn "\.decay(" …` → **0**. So `BujoMemoryStore.reflect()` (store.ts:666), `BujoMemoryStore.decay()` (store.ts:816) and `db.applyDecay()` (db.ts:1988) are **uncalled public-API no-ops** — not "reachable via the CLI path" as §4a's store.ts:666 row implies (the shipped `memory-bujo reflect` CLI calls the **free** `reflect()` function at `cli.ts:104`, not the store method). Keeping them as documented no-ops for v1 published-API stability is reasonable; they are candidates for a v2 removal audit, not dead-weight to cut before freeze. Recorded as `deadCodeUnproven=3` (suspected-idle-but-public, removal not proven safe during v1). |

---

## Part 4a — The 3 (actually 4) `@deprecated` markers

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| reflect.ts:24 `reflect()` | **CONFIRMED** | load-bearing-compat | Free function is invoked by the shipped `memory-bujo reflect` CLI (`cli.ts:100-113`), which injects an LLM that throws if called — a true read-only no-op probe. Reachable, correctly labeled. |
| store.ts:666 `store.reflect()` | **AMENDED** | load-bearing-compat → *idle public-API no-op* | Zero in-repo callers (grep above). Kept for published-API stability, not "reachable via the CLI" (the CLI uses the free function). Keep for v1; flag for v2 removal audit. |
| db.ts:1980/1988 `applyDecay()` | **CONFIRMED** | load-bearing-compat (idle public-API no-op) | Verified permanent no-op returning `{decayed:0}`; zero callers. Auditor's own "not called by any in-scope production path" is accurate. |
| **store.ts:816 `decay()` (NEW-3)** | **AMENDED** (omitted from §4a table) | load-bearing-compat (idle public-API no-op) | A **4th** `@deprecated` marker the §4a table missed (`grep -rn "@deprecated" packages/*/src` → reflect.ts:24, store.ts:666, store.ts:816, db.ts:1980). §3 prose does name "decay()", so it's a table-completeness gap, not a correctness error. Verified no-op; zero callers. |

---

## Part 4b — The 61-file legacy/back-compat ledger

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| §4b whole ledger (0 removable-now / 57 load-bearing / 4 naming-only) | **CONFIRMED** | n/a | The 4 "naming-only-false-positive" calls (#4, #34, #46, #59) are the *conservative* classification (assert MORE reachability, never propose removal) — no removal risk if slightly off, so not independently re-proven line-by-line. Spot-checked the strongest load-bearing claims and all are genuinely in-path: **#17** `LEGACY_TOOL_ALIASES` consumed by `adapter-send-tools.ts:24/1478` + `known-tools.ts:61/83/93` (`canonicalToolName`/`isKnownTool`); **#36/#50** `repairLegacySalience` called in-path at `capture-outbox.ts:686`; **#8/#9** continuation-store `legacy_missing` is a live state-machine member (`continuation-store.ts:64/1467/1528`, `continuations.ts:179`). No misclassified-dead entry found. |

---

## Part 5 — Actionable steps (§5)

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| A1-1 (gitleaks Telegram rule + regression fixture) | **CONFIRMED** | S / P2 | Concrete; acceptance-check (plant synthetic Telegram-shaped token → assert nonzero `gitleaks dir` exit) is exactly what I ran and it works. File-able as-is. |
| A1-2 (dependency-vuln scanning in CI) | **CONFIRMED** | S / P2 | Concrete; acceptance-check valid. |
| A1-3 (`--provenance` / OIDC trusted publishing) | **AMENDED** | S / P3 | Actionable, but **dedupe against existing OPEN issue #230** — comment/expand it rather than open a new one. Acceptance-check (`npm view <pkg> --json` shows provenance) is valid. Add the framing that internal build-provenance already exists; this adds the *public sigstore* layer. |
| A1-4 (set `minimumReleaseAge` or drop the exclude) | **CONFIRMED** | S / P3 | Concrete; either acceptance-check (`pnpm config get minimumReleaseAge` nonzero, or exclude block gone) is verifiable. |
| A1-5 (`chmod 600` the 6 world-readable live files) | **CONFIRMED** | S / P3 | Concrete; `stat -f "%OLp"` acceptance-check valid. Note: **ops task on live instances, not a repo PR / DoD item** — track outside the freeze checklist. |
| A1-6 (doc: root dev config must use `tokenEnv` indirection) | **CONFIRMED** | S / P3 (minor) | Concrete doc note; optional lint clearly marked opt-in. Fine. |

---

## Freeze-blockers

**Confirmed freeze-blockers:** none.
**Rejected freeze-blockers:** none proposed by E4 — every §5 action row is marked `Freeze-blocking = n`, and §1/§2 raise no freeze-blocker. I independently agree: F1 is the most severe item and it does **not** block a DoD clause, ship a secret/license/correctness catastrophe, or make "honest ops" false (nothing has leaked; the primary control works; the repo never claims full-coverage scanning). Two-key rule satisfied: nothing to confirm.

---

## NEW findings (discovered while re-reading code)

- **NEW-1 — `gitleaks dir` ignores `.gitignore` (strengthens F1).** Empirically: a bare 35-char secret in a gitignored **untracked** file is still flagged (`leaks found: 1`), identical with/without the `.gitignore`. Consequence: the real untracked root `mono-agent.config.json` **is** scanned on every CI/local `check:secrets` run and silently passes because of the regex gap — the "no leaks found" is not a skipped file. Makes F1's false-confidence concern concrete (the gate touches the real token every run and waves it through), while confirming F2 is not a *committed* leak.
- **NEW-2 — F4 duplicates already-tracked OPEN issue #230** (cross-artifact, folded into F4 above). E4 presents the npm-provenance gap as novel; E2's F7 correctly identifies it as tracked and honestly disclosed. Any synthesis must merge these into one P3 line referencing #230, not two findings at different severities.
- **NEW-3 — 4th `@deprecated` marker `store.ts:816 decay()` omitted from §4a's table** (folded into Part 4a). Classification unaffected (idle public-API no-op).

## Cross-artifact contradictions

- **E4 F4 (P2) vs E2 F7 (P3)** on the identical npm-provenance/OIDC gap. E2 is right: it is a known, tracked (#230), honestly-disclosed limitation → **P3**. Resolved in favor of E2.
- **E2 §2 corroborates E4's positives:** E2 independently verifies `check-secrets.mjs:8`'s gitleaks invocation and that `.gitleaks.toml`'s allowlist regexes only exempt known-fake fixture placeholders (no live-secret exemption) — consistent with E4 §1. No conflict there.
