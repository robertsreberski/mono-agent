# V9 — Adversarial verification of cluster: live instances (F1-live-personal-agent, F2-live-a8c-fleet)

Verifier stance: refute, not ratify. Every finding, dead-code entry, deprecation classification,
and actionable row below was re-checked against the actual files/live state. Live instances were read
strictly read-only. No secret values are quoted.

Headline: **both artifacts propose ZERO freeze-blockers** (every actionable row marked `Freeze-blocking: n`).
I independently agree: everything here is instance-level operational rough-edge or framework-fit
*observation* — none blocks a v1 framework DoD clause, ships a secret/license/correctness catastrophe,
or makes "honest ops" categorically false. Evidence quality in both artifacts is unusually high; almost
every cited line/byte-count reproduced exactly. Corrections are severity/scope nuances, not fabrications.

---

## Part A — F1-live-personal-agent

### Findings

| item | verdict | corrected sev | correction / proof notes |
|---|---|---|---|
| F1 (1.23GB unrotated stderr) | CONFIRMED | P1 (borderline P2) | `stat` = exactly **1,233,420,489 bytes** for `com.mono-agent.personal-agent-059657c8.err.log.1`; current `err.log` = 656,383 B; `out.log` = 2.36MB, `out.log.1` = 7.76MB. Plist has `StandardOutPath`/`StandardErrorPath` with **no** rotation directive (launchd never rotates by default). CONFIRMED. Sev note: unbounded-growth trajectory justifies P1, but zero operational impact to date (disk has room) makes it arguably P2 hygiene. The "three code-provenance eras" parenthetical enumerates only two contexts — cosmetic imprecision, immaterial. |
| F2 (admin/watchdog tooling not pinned to serving snapshot) | CONFIRMED | P1 (borderline P2) | Plist runs pinned snapshot `~/.mono-agent/runtimes/agent-app/**0.11.2**/darwin-arm64-abi-137/.../@mono-agent/agent-app/dist/cli.js`. `bin/mono-agent:7`, `bin/agent-watchdog:25` (`const CLI = '…/Personal_Repositories/mono-agent/packages/agent-app/dist/cli.js'`), `bin/session-web:5` all hardcode the **mutable monorepo dist**. Verified line-exact. No live drift today (both 0.11.2). Real latent hazard on this specific machine (framework repo == dev machine): a broken WIP `pnpm build` would decouple the every-15-min watchdog health-check (`mono-agent memory audit --strict`) from what's actually serving → false red/green + possible spurious restart of a healthy service. Note: `bin/mono-agent`'s own comment says it deliberately tracks the checkout for Node-ABI parity, so intent is partly by-design; P1 defensible on the dev-machine coincidence, P2 in general. |
| F3 (chronic getUpdates churn, invisible) | CONFIRMED | P2 | Current `err.log`: **276** "Telegram channel degraded" restart events + 1176 `getUpdates` lines over ~6 days (~46/day); `.err.log.1` carries the bulk (auditor's ~4,254). Order-of-magnitude "~130/day over 5 weeks" holds. Self-heals every time (no confirmed message loss). Watchdog checks process-liveness only — churn rate is grep-only. CONFIRMED. |
| F4 (quiet-hours "silent" promise not kept on heartbeat/callback path) | CONFIRMED | P2 (narrow live exposure) | `skills/telegram-notifications/SKILL.md:22-23` promises 23:00–07:00 Europe/Rome deliveries land silently. `cron/heartbeat.md` + `cron/heartbeat-final.md` both `notify: false`; heartbeat schedule `15,45 7-21 * * *` + `0 22 * * *` = 07:15–22:00, so it does **not** overlap quiet hours (auditor's "masked by coincidence" — confirmed exact). `bin/lib/telegram-alert.mjs` `sendNativeTelegramCard` posts a raw `fetch` to `api.telegram.org/…/sendMessage` (line 127) and **never** sets `disable_notification`; grep for `disable_notification|quiet|Europe/Rome` in that file = 0 hits. So the live gap is real but bounded to callback re-deliveries during quiet hours (Robert's own taps); heartbeat cron itself is safe today. Genuine written-promise-vs-code contract gap in the attention-protection premise clause. |
| F5 (voice-capture MCP fallback broken) | CONFIRMED | P2 (framework mcpRequestContextServers territory) | Current `err.log`: **34** × `transcribe-mcp: fatal: missing trusted producing conversation context` + **24** × `mcp_init_failed … Connection closed`; `.err.log.1` (prior 3 weeks) = **0** → recent regression, correctly correlated to the `mcpRequestContextServers` request-context feature. Fails loud (logs fatal), does not fake success. See NEW-3: this is plausibly a **framework** trust-context propagation bug for channel-originated turns, not just instance config. |
| F6 (historical cron overlap/skip rate, invisible) | AMENDED → PLAUSIBLE | P3 | Mechanism CONFIRMED: framework's documented cron `overlap: skip` semantics; skip events surface only in raw logs. Exact historical tallies (focus-scan-hourly 59/18, etc.) **not independently reproduced** — the pre-2026-07-10 counts live in a rotated log and the current `out.log` entries are giant per-run JSON blobs that defeat cheap `grep -c`. Auditor's own framing ("was a problem, now healthy 22/1") makes this a retrospective observability note, not a live defect. Downgrade from implied-P2 to P3. |
| F7 (openaiApi + Session Web on 0.0.0.0 vs tailnet webhook) | CONFIRMED | P3 | Config: `openaiApi.host "0.0.0.0"` + `allowNonLoopback true` (lines 106-111); webhook `host "100.64.103.59"` (line 98). `lsof`: pid 12739 `*:4312 (LISTEN)` (openaiApi), pid 6431 `*:4599 (LISTEN)` (session-web), pid 6460 `100.64.103.59:4315` (webhook, tailnet-bound). Both open surfaces are bearer-token-auth'd; asymmetry with the webhook's own stated rationale is a conscious, documented choice. P3 correct. (Minor: finding cited webhook `:4313`, live port is `:4315` — immaterial.) |
| F8 (retired heartbeat-prefilter partially still imported) | CONFIRMED | P3 | `bin/agent-watchdog:10` live-imports `readHeartbeatState` from `./lib/heartbeat-prefilter.mjs`; `runHeartbeatPrefilter()` throws (line 34); `shouldRunHeartbeat` used only by the dead binary + tests. Verified by reading the file. Legibility nit. |

### Dead code

| item | verdict | proof notes (grep commands + hits) |
|---|---|---|
| `bin/heartbeat-prefilter` (binary) | CONFIRMED dead | `grep -rn heartbeat-prefilter ~/personal-agent` (no include filter, excl node_modules/.git) → referenced only by `launchd/com.user.personal-agent-heartbeat.plist:10` (a plist the installer's `RETIRED_PLISTS` actively `bootout`+`rm`s), `tests/heartbeat.test.mjs:31`, `tests/docs-contract.test.mjs:140` (guard). `launchctl list \| grep personal-agent-heartbeat` = not loaded. |
| `heartbeat-prefilter.mjs`: `runHeartbeatPrefilter`, `shouldRunHeartbeat` | CONFIRMED dead | Only importers are the dead binary + `tests/heartbeat.test.mjs`. |
| `heartbeat-prefilter.mjs` re-exports `readHeartbeatState`/`writeHeartbeatState`/`defaultHeartbeatState`/`HEARTBEAT_MAX_FUTURE_SKEW_MS` "must be kept" | AMENDED | **Only `readHeartbeatState` (the wrapper adding `nativeDeliveries`) is consumed** — by `agent-watchdog:10`. `heartbeat.mjs` imports its `readHeartbeatState`/`writeHeartbeatState` **directly from `heartbeat-state.mjs`**, not via this file. So `writeHeartbeatState`/`defaultHeartbeatState`/`HEARTBEAT_MAX_FUTURE_SKEW_MS` re-exports here are themselves **unused** → cleanup is *easier* than the auditor states (only the wrapper needs relocating). |
| `launchd/com.user.personal-agent-heartbeat.plist` | CONFIRMED dead | Points at `bin/heartbeat-prefilter` (line 10); never staged by installer; only removed by filename. Not loaded. |
| `~/personal-agent/--limit/` (empty wacli store) | CONFIRMED (instance) | Dir present; gitignored (`.gitignore:18 --limit/`). Third-party `wacli` artifact, not framework. |
| `~/.pi/personal-agent/settings.json` qwen3.6/ollama defaults | UNPROVEN | Not independently re-verified this pass; harmless-if-true (routing governed by `mono-agent.config.json`). Out of framework scope. |
| `.mono-agent/great-triage-2026-07-11.applied.log`/`.before.tsv` | UNPROVEN | Not re-verified; harmless one-off clutter. |

### Deprecation & legacy

| item | verdict | notes |
|---|---|---|
| `webhook/health-metrics.md`, `webhook/heartbeat-evaluate.md` (enabled:false path reservations) | CONFIRMED (keep) | `enabled: false` verified (health-metrics.md:7, heartbeat-evaluate.md:8). Load-bearing reservation. |
| `.mono-agent/heartbeat/state.v2-pre-cutover.json` (rollback artifact) | CONFIRMED (keep) | Documented rollback backup; keep. |
| `.mono-agent/memory/legacy/` (pre-rebuild daily notes) | CONFIRMED (keep) | Audit trail for the 2026-07-14 rebuild; keep. |
| `bin/heartbeat-prefilter` family (removable) | CONFIRMED | The one genuinely removable legacy surface; see Dead code. |
| `memory forget` backups (`.memory-forget-backup-*`, `operator/forget-*`) unbounded | CONFIRMED | `.memory-forget-backup-01e5…` = **40MB**; `operator/forget-2026-07-14` = 8KB. Real unbounded-growth pattern; no pruning policy. |

### Actionable steps

F1-1..F1-6, F1-8..F1-10: concrete, acceptance-checks verify the fix. Confirmed filable as-is.
- **F1-7 — AMENDED acceptance-check.** "`grep -r heartbeat-prefilter` returns no matches repo-wide" is **unsatisfiable** while keeping the intentional resurrection guard `tests/docs-contract.test.mjs:140` (which contains the literal string by design). Scope the grep to `bin/` (excluding the guard) or restate the guard's assertion. Also fold in the Dead-code amendment above (fewer re-exports to preserve than stated).

---

## Part B — F2-live-a8c-fleet

### Findings

| item | verdict | corrected sev | correction / proof notes |
|---|---|---|---|
| F1 (zero crash-loop breaker; brain-core looped 108×) | CONFIRMED | P1 | `grep -c "A8C_BRAIN_ATTENTION_TOKEN must contain at least 32 characters" brain-core.log` = **108** (of 502 lines; matches lines 45-152). `KeepAlive`=`<true/>` + `ThrottleInterval`=`10` verified at `bin/agents:967-969`, `1479-1481`, **and also 1989-1991, 2592-2594** (more plist writers than cited → strengthens). Incident-correlated backup `brain-20260713T103843Z.sqlite.invalid-20260713T125902Z-39832` present (integrity verifier correctly rejected it). Instance-level (bespoke `bin/agents`, not framework launchd) → not freeze-blocking. |
| F2 (Memory v2/BuJo unused fleet-wide) | CONFIRMED | P2 (observation) | `attention-radar`/`delivery-copilot`/`knowledge-steward`: `memory.mode "lite"`, `writeMode "disabled"`, `recallTool.enabled false`. `orchestrator` + `deep-research`: `memory` key **absent**. All durable knowledge routed via bespoke `brain-core`. Architectural framework-fit observation, not a defect (auditor concedes "not a bug in the framework"). |
| F3 (A2A = transport only; reliability hand-built) | CONFIRMED | P2 (observation) | Boundary-verified: `packages/control-broker` contains `outbox.ts`/`reconciliation.ts`/`leases.ts`/`migration.ts`/etc.; substance sound. Observation, not defect. |
| F4 (no framework fleet tooling; 3763-line bin/agents) | CONFIRMED | P2 (observation) | `wc -l bin/agents` = **3763** exactly. Observation. |
| F5 (no shared/canonical skill concept) | CONFIRMED | P3 | Verified: `attention-radar/`, `delivery-copilot/`, `knowledge-steward/` each hold real (non-symlink) copies of `agent-result`/`brain-context`/`context-a8c`/`report-back` (`skills-shared/` also carries `gws-a8c`). `validate-fleet.mjs:247-266` re-reads `skills-shared/*` and diffs to catch drift. CONFIRMED. |
| F6 (`@mono-agent` version hardcoded "in 6 files") | AMENDED | P3 | Verified literals: `agent-host.mjs:22` (`EXPECTED_MONO_AGENT_VERSION`), `mono-agent-lock-policy.mjs:4`, `verify-live-fleet.mjs:13`, `validate-fleet.mjs` (≥6 occurrences), `bin/agents` (9 occurrences). Distinct files = **5** (scripts+bin), not 6; the "6th" is presumably `package.json`/`pnpm-workspace.yaml` catalog which is the *legitimate source* to derive from (exactly what F2-5 proposes). Substance holds; count is a slight overcount. Fails closed on mismatch (safe). |
| F7 (~1.5GB dead weight from 3 retired agents) | AMENDED | **P2 → P3** | Sizes CONFIRMED: activity-digest 487M + p2-watcher 501M + slack-sweep 541M = ~1.53GB; absent from root `pnpm-workspace.yaml`; not launchd-loaded; retired plists carry `.retired-a8c-fleet` suffix. **Severity deflated**: these are actively *guarded* as legacy by tooling (see NEW-1), not a live confusion/correctness risk — pure disk hygiene with a documented (if undated) soak policy. **Disposition also amended** (NEW-1): dirs contain **git-tracked** source (11 files each) and are referenced by name in `validate-fleet.mjs`/`v2-migration-snapshot.mjs`/`bin/agents`; the mtime-16:48 "something keeps them version-current" inference is likely just a git checkout touching tracked files, not an active refresh daemon. |
| F8 (empty `legacy/` dir) | CONFIRMED | P3 | `ls -la legacy/` = 0 entries (`du -sh` = 0B). Undocumented. |
| F9 (overdue retryable outbox item, no push alert) | CONFIRMED | P2 | **Still live and STRENGTHENED**: `data/control-broker/result-outbox/pending/3fa4c69….json` still present at re-check, `lastError.code=destination_channel_unavailable retryable=true`, `nextAttemptAt=2026-07-15T13:54:25.752Z` — now ~8h past its own retry window (nextAttemptAt frozen in the past → arguably the retry is stuck, not merely late). `control-broker.log` has 23 `degraded` entries (broker correctly flags it); nothing pushes it to Robert. Acked=111 / delegation tasks=76 (auditor's 110/75 +1 each → live usage moved, consistent). |
| F10 (specialist logs show no task activity) | CONFIRMED | P3 | `wc -l` on attention-radar/delivery-copilot/deep-research/knowledge-steward logs = **27** each; all host-restart lines. Real work only in `data/control-broker/delegation-ledger/tasks/` (76) + `result-outbox/acked/` (111). Legible-sessions gap. |

### Dead code

| item | verdict | proof notes |
|---|---|---|
| `activity-digest/`, `p2-watcher/`, `slack-sweep/` (~1.5GB) | AMENDED | Not launchd-loaded, excluded from workspace — CONFIRMED inert. **But**: `git ls-files activity-digest/` = 11 **tracked** files (only `node_modules` is gitignored, per `git check-ignore`), and they're referenced by name as `legacyAgents`/`LEGACY` in `validate-fleet.mjs:16,612`, `v2-migration-snapshot.mjs:151`, `bin/agents:64`. So "dead weight, just `rm -rf`" is incomplete — disposal is a `git rm` plus retiring/adjusting those guard references. |
| `legacy/` | CONFIRMED dead | Empty; unreferenced except generic "retired directories" doc language that actually points at the three dirs above. |
| `orchestrator/mono-agent.config.json.bak-pre-0.6.0` | CONFIRMED dead | `git check-ignore -v` → matched by `.gitignore:22 *.bak-*` (untracked); mtime Jul 8. |
| Brain Core legacy `/mcp/agent` endpoint | CONFIRMED (at boundary) | `grep /mcp/agent` across all 5 worker configs = **0 hits** → confirmed no active worker uses it. Endpoint's existence inside `packages/brain-core/src` not independently opened (boundary scope per task) → remove only after in-src check, as the finding itself advises. |

### Deprecation & legacy

| item | verdict | notes |
|---|---|---|
| `A8C_GWS_*` aliases for `A8C_ACTION_*` (one-release compat) | CONFIRMED | `bin/agents:79-80,1203-1204` read both and fail closed on mismatch. Load-bearing, removable next release. |
| `a8c_gws_stage_action` tool alias / Slack `a8c_gws_*` block IDs | PLAUSIBLE | Doc-based (README:77,115); not independently exercised. Low-risk compat pattern. |
| Legacy `/mcp/agent` endpoint | CONFIRMED | Removable-now candidate; see Dead code (boundary). |
| V1 broker migration tooling (`migrate-v2`) | CONFIRMED (keep) | Only sanctioned v1-recovery path; keep until zero v1 records remain. |
| `.retired-a8c-fleet` manifests / `legacy:stop` | CONFIRMED (keep) | The `.retired-a8c-fleet` (and `.retired-a8c-custom-host.*`) plists are present in `~/Library/LaunchAgents`; audit-trail only, no `legacy:start`. Keep. |
| `mono-agent.config.json.bak-pre-0.6.0` | CONFIRMED | See Dead code. |

### Actionable steps

F2-1 (crash-loop breaker), F2-2 (page on breaker/overdue), F2-4 (legacy dir), F2-5 (single version source),
F2-6 (surface per-task activity), F2-7 (doc the memory-scope decision): concrete, acceptance-checks verify.
Filable as-is.
- **F2-3 — AMENDED disposition.** Add the NEW-1 caveat: the three retired dirs are git-tracked and named
  in `validate-fleet.mjs`/`v2-migration-snapshot.mjs`/`bin/agents`; disposal must `git rm` and update/retire
  those `legacyAgents` guards, not just `rm -rf`. Acceptance-check should also assert those references were
  cleaned (or deliberately repointed at the external snapshot).

---

## Freeze-blockers

**Confirmed freeze-blockers: NONE.**
**Rejected freeze-blockers: NONE proposed** — both artifacts already marked every actionable row
`Freeze-blocking: n`. I independently agree: all findings are (a) instance-level operational rough edges in
bespoke instance tooling (`bin/agents`, `bin/agent-watchdog`, `bin/lib/telegram-alert.mjs`, launchd plists),
or (b) framework-fit *observations* about where the lean core stops paying off — neither category blocks a
v1 framework DoD clause, and both instances fail loud / self-heal rather than fake success (the a8c fleet
even correctly reported its own 108-restart incident and rejected an invalid backup).

Closest-to-blocker candidates and why they are NOT blockers:
- a8c F1 (108× crash loop): bespoke instance `bin/agents` plist policy, not the framework's launchd path.
- personal F5 (transcribe-mcp fatal): touches a framework feature (see NEW-3) but fails loud, is narrow
  (>2-min Telegram voice notes), and does not corrupt data or fake success. → framework *issue*, not blocker.
- a8c F9 (stuck outbox): retryable, broker correctly flags `degraded`; an alerting gap, not data loss.

## Cross-artifact consistency

- Both instances graded B+; both independently surface the **same framework theme**: no built-in
  ops-observability / alerting / crash-loop protection (personal: watchdog blind to channel churn F3, no log
  rotation F1; a8c: no circuit breaker F1, no push on overdue F9, no task-activity in logs F10). Consistent,
  mutually reinforcing — not contradictory. Worth carrying to synthesis as a single "framework provides no
  honest-ops observability primitives (log-rotation/size caps, crash-loop breaker, channel-health, delivery
  SLO alerting)" theme rather than 6 separate instance nits.
- Both hit the framework's lack of launchd log-size management; personal-agent hit it hard (1.23GB),
  a8c did not (custom host, 27-line logs) — same gap, different blast radius. No contradiction.

## NEW findings (discovered while re-reading code)

- **NEW-1 (actionability/scope refinement, P3).** The a8c retired dirs (`activity-digest`/`p2-watcher`/
  `slack-sweep`) are **git-tracked** (11 files each) and referenced by name in `validate-fleet.mjs:16,612`,
  `v2-migration-snapshot.mjs:151`, `bin/agents:64`. Both artifacts' "just delete the 1.5GB" framing
  (F7 / dead-code / F2-3) is incomplete: disposal is `git rm` + guard-reference cleanup, and the
  "something keeps them version-current" inference over-reads a git-checkout mtime.
- **NEW-2 (actionability bug, P3).** F1-7's acceptance-check `grep -r heartbeat-prefilter` = 0 repo-wide is
  self-contradictory with the intentional resurrection guard at `tests/docs-contract.test.mjs:140`. Restate.
- **NEW-3 (framework-investigation item, P2).** personal F5's `transcribe-mcp: fatal: missing trusted
  producing conversation context` (34× in current log, 0× in prior 3 weeks) is plausibly a **framework**
  bug in `mcpRequestContextServers` trust-context propagation for channel-originated (Telegram) turns, not
  merely instance config — worth an explicit framework issue since any consumer using request-context MCP
  servers could be affected. Not a freeze-blocker (fails loud, narrow surface).
