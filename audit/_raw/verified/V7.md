# V7 — Adversarial verification of E2 (scripts/CI/release) + E3 (repo governance/hygiene)

Verifier: adversarial. HEAD `5f27a0ec`, v0.11.2. Every finding re-checked against source at cited
lines; every dead-code claim independently grepped; live host + GitHub state re-queried read-only.
Grep/command evidence is inline per verdict.

---

## Part E2 — scripts, CI & release lane

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| E2-F1 (no daily automation; counter reset) | **AMENDED** (core CONFIRMED, framing wrong) | **P0** (DoD blocker) | Core CONFIRMED live: `launchctl list \| grep -i green\|fleet` → empty; `crontab -l` → none; `grep -l fleet-green-check ~/Library/LaunchAgents/*.plist*` → no match across 45 plists; the 4 active plists (`finances-e9c073d7`, `inner-child-fdfc3392`, `personal-agent-059657c8`, `transcription-f4a742c8`) exactly match `FLEET_LABELS`. `scripts/fleet-green-check.mjs:39-40` and `skills/fleet-deploy/SKILL.md:153-155` quoted accurately. **BUT the auditor's framing has two factual errors:** (1) "zero comments for 2026-07-13 and 2026-07-14" is FALSE for 07-13 — there IS a 07-13 comment, an explicit *operator cancellation marker* ("The seven-consecutive-day condition did not pass… temporary 21:00 tracker was unloaded before its next run"), not a silent gap. (2) So a temporary launchd tracker *did* exist (that is what produced the 19:00Z/21:00-CEST daily comments 07-08→07-12) and was deliberately unloaded — the SKILL.md "installed nightly job" was true temporarily, now stale, not "never installed." Net: the DoD clause *"Fleet validates and runs green for 7 consecutive days on the v1 build"* is genuinely UNMET (operator said so on 07-13; only one green day — 07-15 — exists since). P0 and freeze-blocking stand. |
| E2-F2 (verify:all ⇄ ci.yml gaps both directions) | **CONFIRMED** | P1 | `scripts/verify-all.mjs:8-19` repoGate has no release step; consumer step at :63-78. `ci.yml` runs `release:validate` (:65), `release:pack` (:74), `release:consumer` (:78) but `grep -n "verify:consumers\|verify-consumers" .github/workflows/` → **empty** (CI never gates the consumer fixtures). `check-secrets.mjs:8` = `["dir","--redact","--no-banner","--config",".gitleaks.toml","."]`, functionally identical to the dockerized `gitleaks:v8.30.1` in `ci.yml:44-49` (config path differs `.gitleaks.toml` vs `/repo/.gitleaks.toml`; pinned image vs local PATH — both accurate). NB: cited `verify-green/SKILL.md:15-17` for "one-shot equivalent" is actually **line 24**; substance holds. |
| E2-F3 (CI re-runs demo build/typecheck/test) | **CONFIRMED** | P2 | `build-with-provenance.mjs:31-34` `BUILD_COMMANDS` chains `build:demo`; `package.json:23` typecheck chains `typecheck:demo`; `package.json:25` test chains `test:demo`. `ci.yml:86-93` re-runs all three; `verify-all.mjs:17` has a redundant `test:demo` after `test` (:16). Each demo command runs **twice**; heading "three separate times" is slightly loose (it's 3 commands each doubled) but substance is right. Real CI-minute waste + a hidden-chaining legibility trap. Premise: ops-efficiency, not runtime-core. Not freeze-blocking. |
| E2-F4 (phantom gates: getting-started-pins + deep-imports) | **CONFIRMED** | P2 | `check-getting-started-version-pins.mjs:90-108` has a full `main()`/`isCli`; `grep "getting-started-version-pins" package.json verify-all.mjs .github/workflows/` → **empty** (no named script). Exercised only by `getting-started-version-pins.test.mjs:6-9` calling the real fn with **no overrides**, running via `scripts:test` glob (`package.json:35` = `vitest run scripts/__tests__/*.test.mjs`, chained into `test` at :25). `verify-deep-imports.mjs:12-14` self-declares "part of the phase gate" yet `grep "verify-deep-imports\|check:deep-imports" package.json verify-all.mjs .github/workflows/` → **empty**; only its no-mock test case exercises it. Both real, both invisible to `package.json` readers, both fragile to a test-glob/mocking-discipline edit. Bears on "honest ops"/legible gate. |
| E2-F5 (validate-release ignores root devDep pins; issue #228) | **CONFIRMED** | P2 | `validate-release.mjs:39` reads `rootPackageJson`, uses it only at :52-54 (engines.node); internal-pin loop :82-94 iterates `publishable` only, never `rootPackageJson.devDependencies`. Verified root has **18** `@mono-agent/*` devDeps, all `workspace:0.11.2` (`node -e` count). Already tracked by open #228; accurate as of HEAD. Not freeze-blocking. |
| E2-F6 (fleet-green-check.mjs 2,380-line / ~90-fn outlier) | **CONFIRMED** | P2 | `wc -l` = **2380** exactly; function-count grep ≈ **84** (auditor "~90" close). Legibility finding. Caveat for synthesis: this is an **ops/audit script**, not the runtime "core" the premise's "competent-stranger-reads-the-core" clause centers on — apply the clause with that framing. Heavily unit-tested, so correctness risk is low. Not freeze-blocking. |
| E2-F7 (issues #226/#230 + stage-tag debt; no action) | **CONFIRMED** | P3 | Code-side re-verified independently: `build-with-provenance.mjs:29` `TRUSTED_GIT = "/usr/bin/git"`; `selectBuildInvocation` Windows-descendant constraint at :119-139 present exactly as described. npm-registry claims (no provenance field, 7 stage tags) accepted as auditor's live evidence (low-value to re-pull; no doc falsely claims provenance). Honestly disclosed, non-blocking. |

### E2 dead-code

| path | verdict | proof |
|---|---|---|
| `scripts/memory-capture-baseline-probe.mjs` + `scripts/fixtures/memory-capture-mixed{,.baseline}.json` | **CONFIRMED dead** | `grep -rn memory-capture-baseline-probe . --include=*.{json,yml,yaml,mjs,md,js,ts}` (excl. the file + audit/) → **no matches** (exit 1). `git log --oneline -- scripts/memory-capture-baseline-probe.mjs` → single commit `e15ad34f` (PR #202). Hardcoded `EXPECTED_HEAD = "54f3fb49…"` (line 9) + `throw` at line 16 → unrunnable past that sha (repo is 10+ commits past). Both fixtures present on disk. Delete-or-archive disposition sound. |

---

## Part E3 — repo governance, dev tooling & git hygiene

| item-id | verdict | corrected severity | correction / proof notes |
|---|---|---|---|
| E3-F1 (#119 DoD "≤18 active" false, never corrected) | **AMENDED** | **P1** (reject freeze-blocker) | DoD line quoted accurately from `gh issue view 119` body. Catalog re-counted: publishable **21** = **16 core + 4 plugin + 1 alias** (`node -e` on `package-catalog.mjs`). **AMEND "definitionally false":** the line reads "Package count ≤18 **active**"; under a *core* reading (16 ≤ 18) it is arguably SATISFIED, under a *publishable-total* reading (21) it is not — the honest defect is **ambiguity/staleness**, not flat falsity. Fix = disambiguate + cite the #198 plugin-tier decision (auditor's A1-1 does this). Real must-fix-before-closing-#119, but a doc edit — not a capability/correctness/secret catastrophe. |
| E3-F2 (implementer.md worktree path drift) | **AMENDED** (deflate) | **P2** (reject freeze-blocker) | `agents/implementer.md:21` says `.claude/worktrees/<name>`; `worktree-feature/SKILL.md:14-16` says `~/.config/superpowers/worktrees/mono-agent/`; #119 body item 9 repeats the stale path — all confirmed. `git check-ignore -v .claude/worktrees` → `.git/info/exclude:11` (personal, not shared `.gitignore`); dir empty; `git worktree list | grep -c .claude/worktrees` = **0**. Contradiction is real, BUT risk is **theoretical** — all 44 live worktrees are under `~/.config/superpowers`; nobody follows the stale line. Deflate P1→P2; not a DoD/catastrophe/honest-ops blocker. |
| E3-F3 (git/worktree hygiene regressed since #167) | **CONFIRMED** | P2 | `git branch --list \| wc -l` = **47**, `git branch -r` = **84**, `git worktree list` = **50**, `gh pr list --state open` = **0**. Root cause `gh api …/mono-agent --jq .delete_branch_on_merge` = **false**. Sampled `feat/effort-keyword-escalation`→#208, `fix/goal-255-native-notify-reply`→#256, `fix/a2a-dispatch-idempotency`→#249 — all **MERGED**. #167 regressed within 8 days; no standing automation. Premise: honest-ops/hygiene. Not freeze-blocking. |
| E3-F4 (README 451 lines no longer "lean") | **CONFIRMED** | P2 (partly subjective) | `wc -l README.md` = **450** (auditor 451, off-by-one), `wc -c` = **38,672** (~38 KB). Cited Quickstart internals paragraphs present. Premise-relevant ("lean, understandable core" / "agents in seconds-to-minutes") but the "too dense" judgment is style-adjacent — keep P2, treat as a real-but-subjective legibility item, not a hard gate. Not freeze-blocking. |
| E3-F5 (stale untracked packages/ dirs) | **CONFIRMED** | P3 | `git ls-files packages/mono-agent` = 0, `…/memory-supermemory` = 0; neither has `package.json`; `ls packages/` = 19 dirs, **17** with `package.json`. `extras/memory-supermemory/package.json` exists (the live package moved there) — so no contradiction with E2-F5. Local-only housekeeping. |
| E3-F6 (drift guard misses release-lockstep SKILL prose) | **CONFIRMED (strengthened)** | P2 | `package-count-drift.test.mjs:21-76` guards exactly `website/README.md`, `PACKAGES.md`, `README.md` (9 entries); `skills/release-lockstep/SKILL.md:14-18` hardcodes "21 / 16 core / four plugin-tier extras" and is NOT guarded. **Strengthened:** SKILL.md:20 self-asserts "release:test's package-count-drift check guards the tier and total counts" — a guarantee it claims over prose that is not actually guarded (see NEW-2). Not freeze-blocking. |

### E3 dead-code

| path | verdict | proof |
|---|---|---|
| `packages/mono-agent/` (dist+node_modules only) | **CONFIRMED dead** | `git ls-files packages/mono-agent` = 0; no `package.json`; `pnpm-workspace.yaml` `packages/*` glob needs a manifest → inert. Superseded by `packages/create-mono-agent` (PR #175). Local-only `rm -rf`. |
| `packages/memory-supermemory/` (dist+node_modules only) | **CONFIRMED dead** | `git ls-files …` = 0; no `package.json`; live package is `extras/memory-supermemory/` (verified present). Old build output from before #198. Local-only `rm -rf`. |
| `.claude/worktrees/` (empty dir) | **CONFIRMED (cosmetic)** | Empty (`ls -la` → only `.`/`..`); excluded via `.git/info/exclude:11` not `.gitignore`; 0 worktrees rooted there. NB: removing the empty dir is cosmetic — the load-bearing fix is the stale doc references in E3-F2, not the directory. |

### E3 deprecation & legacy

Accepted as CONFIRMED without per-line re-verification (low value, clearly correct on read): the three
back-compat surfaces (`--fallback-models` CSV, `runtime.fallbackModels` key, `recipes`/`--recipe` alias)
and the npm-web-UI deprecation *process* note are all honestly-disclosed, still-functional, load-bearing
aliases — none removable now without a breaking change, none stale references to removed surfaces. No action.

---

## Confirmed freeze-blockers

- **E2-F1 / E2-1 — the v1 DoD clause "Fleet validates and runs green for 7 consecutive days on the v1 build" is UNMET, with no currently-running daily automation.** This is the one true freeze-blocker in this cluster: verified live (no launchd/cron job), and the operator's own 07-13 comment states "The seven-consecutive-day condition did not pass." Second key granted. **Scope it as the DoD outcome, not merely "install a plist"** — the blocker is *achieving* an unbroken 7-day green window on the actual freeze commit; the daily job (E2-1) is the enabling means. See NEW-1: it must restart on the frozen sha.

## Rejected freeze-blockers (downgraded — second key withheld)

- **E2-2 (wire `verify:consumers` into CI) → P1.** The consumer fixtures **pass today** via `verify:all`; the gap is future-regression risk, not a current falsity, DoD clause, secret, or correctness catastrophe. Worth doing before freeze; not blocking.
- **E3-A1-1 (correct #119 DoD package-count line) → P1.** A real must-fix-before-closing-#119 doc edit, but trivial and (under a core=16 reading) arguably already satisfied; not a capability/correctness/secret/honest-ops-runtime blocker.
- **E3-A1-2 (fix `implementer.md` worktree path) → P2.** Dev-tooling doc drift with only theoretical risk (nobody follows the stale line); not a DoD/catastrophe/honest-ops blocker.

## NEW findings (discovered during re-read)

- **NEW-1 (amplifies E2-F1) — the fleet is running a sha 2 commits behind HEAD, so the 7-day clock cannot even start on the current tip.** The only green-check on record (07-15) reports `Deployed sha: 4ebd8e0` = the v0.11.1 merge (`4ebd8e02`); `git log 4ebd8e02..5f27a0ec` = **2 commits** (`218ec3bd`, `5f27a0ec`/v0.11.2). The DoD requires 7 consecutive green days *on the v1 build*; before the window can begin, the freeze commit must be chosen and the fleet redeployed to it. Synthesis should sequence: pick freeze sha → redeploy fleet → install daily job → run 7-day window. (Severity: folds into the P0 E2-F1 blocker.)
- **NEW-2 (strengthens E3-F6) — `skills/release-lockstep/SKILL.md:20` claims a guard it does not have.** The line asserts "`release:test`'s package-count-drift check guards the tier and total counts," yet the SKILL.md's own count prose (`:14-18`) is absent from `guardedPackageCountReferences`. A release-engineer subagent reading this contract is told its numbers are drift-protected when they are not. P3; fixed by A1-8.

## Cross-artifact contradictions

- **None material.** E2 and E3 both touch epic #119 but from complementary angles (E2-F1 on the daily *verdict comment* stream / 7-day window; E3-F1 on the *DoD body* package-count text) — consistent, not contradictory.
- **Apparent-but-not-a-contradiction flagged for synthesis:** E2-F5 says the root `package.json` still pins `@mono-agent/memory-supermemory` (`workspace:0.11.2`), while E3-F5/dead-code calls `packages/memory-supermemory/` dead. Both are correct — the *package* moved to `extras/memory-supermemory/` (verified present), so the root devDep resolves there; only the stale `packages/memory-supermemory/` **build output** is dead. Do not read these as conflicting.
