# V5 — Adversarial verification of cluster C4–C7 (tui/session-web, messaging, ingress, config/scaffolding)

Verifier: adversarial (refute-first). Every cited line was opened; every dead-code + removable-deprecation claim was independently proof-of-death'd with grep across `packages extras demos scripts website` and both live instances (`~/personal-agent`, `~/a8c-agents`), excluding `node_modules`/`dist`. HEAD 5f27a0ec, v0.11.2.

**Headline results:** 0 confirmed freeze-blockers (all 3 proposed rejected under the two-key rule). 28 findings confirmed as-stated, 3 amended (2 severity downgrades + 1 major reframe), 0 fully refuted. 6 dead-code claims proven. 1 material NEW finding (C6-F1's overlap feature is product-unreachable, which reframes that finding) + 2 minor NEW nits.

---

## Part C4 — tui & session-web

| item | verdict | corrected sev | notes (evidence / proof) |
|---|---|---|---|
| F1 replay-detail degrades `session_boundary` to raw JSON | CONFIRMED | P2 | Verified `replay-detail.ts:194-196`: `runtime` category returns a cell only for `runtime_warning`; every other runtime item (incl. `session_boundary`) returns `undefined` → raw payload. Sibling `turn-presenter.ts:147,327,329,343` has `sessionBoundaryNotice` producing "session boundary: <kind>". Real asymmetry; legible-sessions relevant. |
| F2 webapp React render trees untested | AMENDED | **P1→P2** | Proof holds: no `testing-library` in `webapp/package.json`, zero `render(` in any webapp `*.test.ts` (grep both empty). But this is a *regression-protection* gap, not a present defect — the render logic works today. P1 is reserved for shipped defects; a pure coverage gap on a working surface is P2. |
| F3 markdown `dangerouslySetInnerHTML` untested | CONFIRMED | P2 | `markdown.ts:19` `mdInline` does `let s = esc(input)` FIRST then applies tags; `:92` injects `md(src)`. Escape-then-wrap order is correct → **not a live vuln today** (auditor agrees); finding is coverage-only. P2 justified by safety-sensitivity. |
| F4 dead: `TuiAgentCancelledError`/`isTuiAgentCancelledError` | CONFIRMED | P3 | PROVEN — see dead-code table. Caveat: these are exported *public API* (`index.ts:9-10`); "dead" = zero internal/monorepo consumers. Keeping as intentional embedding-host surface is a valid alternative disposition. |
| F5 dead+stale `TUI_PACKAGE_VERSION` | CONFIRMED | P3 | PROVEN — value `0.1.0` vs `package.json` `0.11.2` (verified). Only def + re-export, never read. |
| F6 Config view/pane no dedicated test | CONFIRMED | P2 | `ls packages/tui/src/__tests__/ \| grep -i config` → nothing. Honest-ops preview surface; coverage gap on a working surface. P2 defensible (borderline P3). |
| F7 dead: `listInstanceSessions` | CONFIRMED | P3 | PROVEN — `history.ts:105` def + only `history.test.ts` callers; NOT in `session-web/src/index.ts` (grep confirmed absent). Test-only. |
| F8 auth token persists in `localStorage` | CONFIRMED | P3 | Accepted per auditor's read of `api.ts:307-322`; honest-ops doc gap (README omits the persistence choice). P3 fine for a read-only Tailscale-node console. |
| F9 `--responder` embedding path untested | CONFIRMED | P3 | Accepted; documented public capability with no test evidence. P3. |

**Dead-code (C4):**

| symbol | verdict | grep proof |
|---|---|---|
| `TuiAgentCancelledError`/`isTuiAgentCancelledError` (`tui/src/agent/responder.ts:17,27`) | PROVEN dead (internal) | `grep -rn "TuiAgentCancelledError\|isTuiAgentCancelledError" --include=*.ts packages extras demos scripts website` (excl. node_modules/dist) → 13 hits = def (responder.ts), re-export (index.ts:6,9,10), and an *independently re-declared same-named class* in `agent-contracts/__tests__/contracts.test.ts:78` (not an import). No real consumer. `chat.ts` uses base `isAgentResponseCancelledError`. Live instances: N/A (internal TS symbol). |
| `TUI_PACKAGE_VERSION` (`tui/src/runtime/version.ts:6`) | PROVEN dead | grep → only `version.ts:6` (def) + `index.ts:75` (re-export). Never read. |
| `listInstanceSessions` (`session-web/src/history.ts:105`) | PROVEN dead | `grep -rn "listInstanceSessions\b"` → def + `history.test.ts` only; absent from `index.ts`. |

**Deprecation (C4):** 2 back-compat surfaces (`listReplayRuns(number)` overload; `WebInstance.timezone` mirror) classified load-bearing/keep — CONFIRMED reasonable (both public, cheap, tested).

**Actionable steps (C4):** A1-1..A1-5, A2-1..A2-4 all concrete with real acceptance checks. A1-4's acceptance ("`--version` prints real version") is correctly conditional. No rewrites needed.

---

## Part C5 — telegram & slack adapters

| item | verdict | corrected sev | notes (evidence / proof) |
|---|---|---|---|
| F1 disabled-channel structural config still hard-validated (both adapters) | CONFIRMED (freeze-blocker REJECTED) | P1 | Verified telegram `config.ts:185` `readTelegramSendTools(json)` runs before the `if(!enabled)` early-out at `:190`; `readTelegramSendTools` (`:246-262`) throws `invalidConfig` on unknown fields / bad scope. Slack `config.ts:149-150` `readSlackShortcuts`/`readSlackHomeTab` run before `enabled` read `:151` / early-out `:171`; both throw on malformed input (`:273`, `:335`). Contradicts the documented invariant in both. Real, doubled, untested. **Severity P1 upheld** (doubled + documented-invariant violation). **Freeze-blocker rejected**: fails loudly and names the field (honest, not dishonest); narrow trigger (malformed advanced structural block + disabled); no DoD clause requires disabled channels to skip structural validation; not a correctness/secret catastrophe. |
| F2 READMEs advertise removed `telegramFieldGroup`/`slackFieldGroup` | CONFIRMED | P2 | `grep FieldGroup packages/*/src/index.ts` → nothing; actual exports are `TELEGRAM_CONFIG_FIELDS`/`SLACK_CONFIG_FIELDS` (`index.ts:107`/`:102`). README `telegram:44,55` / `slack:36` still cite the removed names. Copy-paste import would throw. |
| F3 `showThoughts` dead no-op threaded end-to-end | CONFIRMED | P2 | See dead-code table. `TelegramMessageStream` constructor (`message-stream.ts:252-307`) reads maxMessageChars/formatMarkdown/initialStatusText/editDebounceMs/... but NEVER `options.showThoughts`; `channels.ts:1440` hardcodes `showThoughts:true`. |
| F4 `redactSlackAdapterConfig` dead exported | CONFIRMED | P3 | See dead-code table. Zero external consumers; parallel to app's generic field-spec redaction path. Drift risk is real but low. |
| F5 Slack has no quiet-hours/silent parity | CONFIRMED | P3 | `grep silent\|quietHours\|disable_notification slack-adapter/src` → every "silent" hit is about a *silently dead socket* (`start.ts:66`, `socket-mode-runner.ts:77,424,440`) or "silently dropping" — none is a notify flag. `SlackNotifyOptions` (`adapter.ts:121`) has only `verbatim`/`deliveryKey`. Parity gap; premise-relevant ("channels easily"). |
| F6 Slack error-text string-only vs Telegram callback | CONFIRMED | P3 | Accepted; minor asymmetry between flagship channels. |
| F7 Slack has no `log-redaction` analog | CONFIRMED | P3 | Accepted; no active leak today, defense-in-depth parity gap. |

**Dead-code (C5):**

| symbol | verdict | grep proof |
|---|---|---|
| `showThoughts` (telegram `adapter.ts:190`, `bot.ts:1255-56`, `message-stream.ts:49`) | PROVEN dead (no read site) | `grep -rn showThoughts packages --include=*.ts` (excl tests/dist) → 5 hits: 2 type decls, 1 forward (`bot.ts`), 1 app set-true (`channels.ts:1440`); NO read in `message-stream.ts` constructor (read `:252-307` in full — confirmed absent). |
| `redactSlackAdapterConfig` (`slack-adapter/config.ts:432`) | PROVEN dead (no external consumer) | `grep -rn redactSlackAdapterConfig` (repo-wide) → only `config.ts:432` def, `index.ts:101` export, `config.test.ts` (self test). Nothing outside `slack-adapter/`. Telegram sibling has exactly one consumer (`demos/final-agent`). |

**Deprecation (C5):** none marked `@deprecated`; `showThoughts` is soft-deprecated-in-JSDoc but not scheduled — CONFIRMED (folded into F3/dead-code).

**Actionable steps (C5):** C5-3..C5-7 concrete and correct. **C5-1 and C5-2 change "Freeze-blocking: y" → "n"** (see rejected list). Their acceptance-checks are otherwise good (assert `loadXConfig` resolves instead of throwing for disabled+malformed). C5-6's "how" honestly notes Slack may have no native silent-post equivalent — good.

**PR #243 focus-question validation** (getUpdates timeout→restart loop): ACCEPTED. The auditor's account (15s→90s runner budget kept under the 120s watchdog; recovery detection now requires a real successful getUpdates from the same runner generation) is internally consistent and backed by named tests (`start.test.ts:173,571,775,940`). Not independently re-derived (positive validation, not a finding to refute); no red flags.

---

## Part C6 — webhook, cron, openai-api & operator adapters

| item | verdict | corrected sev | notes (evidence / proof) |
|---|---|---|---|
| F1 cron README contradicts shipped overlap feature | **AMENDED** (freeze-blocker REJECTED) | **P1→P2** | The README-vs-package-API contradiction is REAL (`README.md:34` "skipped, not queued"; `:87` "does not... queue"; but `scheduler.ts:55` `CronOverlapMode="queue"\|"skip"\|"replace"` + tested at `cron-adapter.test.ts:223,296,347,416,551`). **BUT the auditor overstated impact.** Proof: `overlap`/`maxQueueDepth`/`overflow` are NOT config-file-reachable — `CronJobConfig` (`config.ts:17-32`) has no such field, the loader/`jobs-dir.ts`/`toCronJobs()` never parse or project them, and `agent-app/channels.ts:1057-1063` **hardcodes `overlap:"skip"`** with a comment "(Queue/replace remain available to programmatic callers of startCronAdapter)". So for every mono-agent product user, cron IS skip-only, and `docs/channels/cron.md:126-131` + the config schema (no `overlap` key) are HONEST. The bug is confined to the low-level *package* README misdescribing its *programmatic* API for direct embedders (who see the real API via TS types anyway). P2, not P1. See NEW-1. |
| F2 only openai-api re-validates bound address post-`listen()` | CONFIRMED | P2 | All four call `assertSafeBind` pre-listen (`openai:167`, `webhook:246`, `live:84`, `tui:107`); only `openai-api/server.ts:307-323` rechecks `address.address` after listen. Real cheap-to-fix asymmetry. |
| F3 `toCronJobs()` safety-critical but absent from README | CONFIRMED | P2 | `grep toCronJobs cron-adapter/README.md` → nothing (verified full README read; Public API list `:71-79` omits it). Exported `index.ts:28`; footgun JSDoc "otherwise ... disabled jobs would run." |
| F4 openai sampling params validated but never applied | CONFIRMED | P2 | `grep temperature packages/*/src (non-test)` → ONLY `openai-api-adapter/server.ts`. `grep .parameters agent-app/src agent-harness/src` for openai → nothing. Extracted into `metadata.openaiApi.parameters`, never read. Honest-ops gap (accepted, no signal to user). |
| F5 operator `live/config.ts` zero dedicated tests | CONFIRMED | P2 | `ls live/__tests__/` → only `server.test.ts`; `tui/__tests__/` has `config.test.ts`+`server.test.ts`. `redactLiveAdapterConfig` (secret-redaction) untested. Real sibling asymmetry. |
| F6 openai SSE tool-call rendering has no size cap | PLAUSIBLE | P2 | Asymmetry claim is consistent with the codebase (tui `truncateEvent` bounds NDJSON frames; openai `openWebUIToolDetails` writes raw args/result to SSE). Not line-verified `server.ts:1069-1097` in this pass → marked PLAUSIBLE not CONFIRMED. Low blast radius (openai-api is loopback-default). |
| F7 three READMEs advertise non-existent `*FieldGroup` | CONFIRMED | P3 | `grep FieldGroup` in webhook/cron/openai READMEs → present (`:82`/`:78`/`:96`); in `packages/*/src/index.ts` → none. operator README correctly has none. Leftover from the deleted field-group registry. |
| F8 `isDeliverableConversation` triplicated verbatim | CONFIRMED | P3 | Proven 3 identical copies: `webhook/server.ts:835-836`, `agent-app/channels.ts:1000-1001`, `agent-harness/harness.ts:2667` (inlined). No shared helper. Future-channel drift risk. Spans C1/C2 scope (cross-cluster note). |
| F9 webhook is only HTTP sibling with no built-in auth | CONFIRMED | P3 | Honestly disclosed in README/docs. Real defense-in-depth inconsistency; premise-relevant but disclosed. |
| F10 webhook has no per-endpoint `maxRunMs` override | CONFIRMED | P3 | Accepted; cron has per-job override (`scheduler.ts:429`), webhook has adapter-wide only. Minor parity gap. |

**Coverage-refutation (C6):** The auditor refuted the recon hint (operator/openai ~0.36/0.40) with real `vitest --coverage` (81.76% / 86.86%). ACCEPTED as sound methodology; not re-run (expensive, and it is a positive refutation of a recon estimate, not a finding needing adversarial proof).

**Dead-code (C6):** NONE found; ruled-out candidates spot-checked — `handleTick` is imported by `cron-adapter.test.ts:9` (verified), `notifyFailureCooldownHours` read by `channels.ts:1143`, `Redacted*Config` identity types intentional. Ruling-outs CONFIRMED reasonable.

**Deprecation (C6):** legacy single-endpoint/single-job on-ramp — load-bearing, not removable. CONFIRMED.

**Actionable steps (C6):** C6-2..C6-10 concrete with good acceptance checks. **C6-1 changes "Freeze-blocking: y" → "n"** and its "how" should be REWRITTEN given NEW-1: the fix is not "describe a feature agent-app supports" but "correct the *package* README to say queue/replace are available to programmatic `startCronAdapter` callers only; the agent-app product pins skip." Acceptance-check should also cover `docs/channels/cron.md` staying accurate for the config-file audience (it already is).

---

## Part C7 — config package, create-mono-agent & demo

| item | verdict | corrected sev | notes (evidence / proof) |
|---|---|---|---|
| F1 `IDENTITY.example.md` ships retired memory tool names | CONFIRMED | P2 | `grep` retired names → `IDENTITY.example.md:10-11` (`journal_append`/`memory_search`/`entity_get`/`memory_read_day`/`memory_list_days`). Current surface is single `memory_recall`. **Absent from both live instances** (grep `~/personal-agent`, `~/a8c-agents` → 0 — the fleet already migrated). Onboarding-template drift; premise-relevant (clean memory). Minor imprecision → NEW-3. |
| F2 `deploy-cli.ts` zero test coverage | CONFIRMED | P2 | `grep deploy-cli\|parseDeployCliArgs demos/final-agent/src/__tests__/` → nothing; only `cli-args.test.ts`/`deployment.test.ts`/`final-demo.test.ts`. Note: demo-scoped (not a published npm package) → lower blast radius than a package parser; P2 defensible but borderline P3. |
| F3 `ultra` effort documented without runtime-inversion caveat | CONFIRMED | P2 | `config/README.md:59-61` lists `ultra` as a valid effort value, no caveat. `effort-keywords.ts:54-57` ranks `ultra`=7 (above `max`=6). `turn-runner.js:147-155` `thinkingLevelForEffort` falls through to `return "low"` for `ultra` (not none/max/xhigh/high/medium) — so `ultra` yields the SAME low thinking as `minimal`/`low`. Genuine honest-ops doc gap. |
| F4 `MEMORY_LLM_ENV_KEYS` duplicated literal | CONFIRMED | P3 | Verified two independent defs: `config.ts:91` (read `:1088`) and `layered-loader.ts:532` (read `:570`). Drift risk real; parity test can't catch two hand-maintained arrays diverging. |
| F5 four `DEFAULT_*_RETENTION_*` exports, zero external consumers | CONFIRMED | P3 | See dead-code table. Accurate framing: not truly dead (used internally `config.ts:465-500`) — dead only as *public exports*. |

**Dead-code (C7):**

| symbol | verdict | grep proof |
|---|---|---|
| `DEFAULT_ARTIFACT_RETENTION_MAX_AGE_DAYS` / `_MAX_COUNT` / `DEFAULT_MEMORY_ARTIFACT_RETENTION_*` (`config.ts:99-102`) | PROVEN dead-as-public-export | `grep -rn` the 4 names across packages/extras/demos/scripts/website (excl node_modules/dist) → only `config/src/index.ts` (re-export) + `config/src/config.ts` (def + internal use `:465-500`). No importer anywhere else. Disposition: stop re-exporting, or wire into doctor/config-reference. |

**Deprecation (C7):** table of 4 (RETIRED_MEMORY_ENV_KEYS, memory.reflection/migration, fallbackModels dual API, `ultra`) all classified load-bearing/keep — CONFIRMED reasonable classifications with accurate evidence.

**Actionable steps (C7):** A1-1..A1-5 all concrete with verifiable acceptance checks. No rewrites needed. A1-3 correctly offers both a README caveat and an optional `console.warn` guarded to fire only on directly-configured (not escalation-derived) `ultra`.

---

## Confirmed freeze-blockers

**NONE.** All three proposed freeze-blockers rejected (two-key rule: I am the second key and I decline all three).

## Rejected freeze-blockers (downgraded)

- **C5-1** (telegram `sendTools` validated before the `enabled` early-out) → **P1 finding, not freeze-blocking.** Fails loudly and names the offending field (honest, not a dishonest/silent-success path); trigger is narrow (malformed *advanced* structural block AND channel disabled); no v1 DoD clause requires disabled channels to skip structural validation; not a correctness/secret/license catastrophe. Real bug worth fixing pre-freeze, but not a blocker.
- **C5-2** (slack `shortcuts`/`homeTab` validated before the `enabled` early-out) → **P1 finding, not freeze-blocking.** Same reasoning as C5-1; identical shape in the sibling adapter.
- **C6-1** (cron README contradicts overlap feature) → **P2 finding, not freeze-blocking.** It is a *package-README* accuracy bug affecting only direct programmatic embedders. The product config-file path (agent-app hardcodes `overlap:"skip"`) and its canonical docs/schema are honest and consistent, so "crons... easily" and "honest ops" are NOT falsified for actual mono-agent users. Not a DoD violation (`overlap` is not a config key). See NEW-1.

## NEW findings (discovered while re-reading code)

- **NEW-1 (P3, reframes C6-1) — the cron `overlap:"queue"/"replace"` + `maxQueueDepth`/`overflow` machinery is fully built and tested but UNREACHABLE from the mono-agent product config surface.** `CronJobConfig` has no overlap field; the config loader, `jobs-dir.ts`, and `toCronJobs()` never parse or project it; and `agent-app/src/channels.ts:1057-1063` hardcodes `overlap:"skip"`. The whole overflow-policy state machine (`scheduler.ts:282-362`) ships in v1 exercisable only by third-party programmatic callers of `startCronAdapter`. This is the *correct* interpretation of C6-F1 (and explains why `docs/channels/cron.md:126-131` legitimately says "skipped, never queued" for the config-file audience). For the v1-freeze "lean core" lens, Robert should decide whether an unreachable-from-config feature belongs in the frozen surface, or whether it should be documented purely as an embedder-only API. Acceptance if kept: cron-adapter README states queue/replace are programmatic-only and the product pins skip.
- **NEW-2 (P3 nit) — `packages/config/src/types.ts:261`** references `memory_search` in a JSDoc comment ("Embedding provider for semantic memory_search; keyword fallback when unset"). Conceptual, not a tool invocation, so it doesn't break anything — but it slightly falsifies C7-F1's claim that the retired names have "zero hits outside this file." Worth a one-word comment tweak when fixing F1.
- **NEW-3 (P3 nit, correction to C7-F1)** — C7-F1's "Grepping the whole repo... returns zero hits outside this file" is imprecise (NEW-2 is a second hit). Does not change F1's validity; the `IDENTITY.example.md` tool names are genuinely retired.

## Cross-artifact / cross-cluster notes

- **Recurring pattern confirmed across C5 + C6:** stale `*FieldGroup` README references (C5-F2 telegram/slack, C6-F7 webhook/cron/openai) all trace to the same deleted field-group registry (per `config-consolidation-recipes.md`). Consistent, not contradictory; a single docs-sync mechanical check (README backtick-identifiers vs `src/index.ts` exports) would catch all five at once.
- **Cross-cluster (C6-F8):** `isDeliverableConversation` triplication reaches into `agent-harness/harness.ts:2667` and `agent-app/channels.ts:1000` — outside this cluster's file scope. Flag for the C1/C2 verifier so the de-dup action is filed once against the right owner.
- No genuine contradictions found *between* the four artifacts.
