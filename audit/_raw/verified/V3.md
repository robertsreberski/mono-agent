# V3 — Adversarial verification of cluster B (memory) — B1, B2, B3

Verifier pass over `B1-memory-capture-recall.md`, `B2-memory-graph-lifecycle.md`,
`B3-memory-store-search.md`. Every numbered finding, dead-code row, deprecation
call, and freeze-blocker was re-checked against the code at HEAD `5f27a0ec`.
Live instances (`~/personal-agent`, `~/a8c-agents`) were read-only; no secrets quoted.

Legend: CONFIRMED = evidence + severity + premise all hold. AMENDED = holds but
severity/reasoning/disposition corrected. REFUTED = does not hold. UNPROVEN =
dead-code/deprecation claim not independently provable.

---

## Part B1 — capture & recall

| item | verdict | corrected sev | notes / proof |
|---|---|---|---|
| F1 strict capture has no fence tolerance; Ollama adapter omits JSON mode | AMENDED | P1→P1 (evidence) but **fails-closed** | Evidence solid: `extractCapturePlanStrict` (capture-batch.ts:103) → `parseJsonExact` (json.ts:36-132) starts at cursor 0 and throws "trailing data" on any leading `` ``` `` — confirmed by reading the parser. `ollama-llm.ts:28` body `{model,prompt,stream}` has no `format:"json"` — confirmed. Wiring confirmed: intake `capture` dep = `captureCompletedTurn` → `captureTurnStrict` (store.ts:973,571). BUT it **fails closed**: deterministic host summary is still written (no data loss), failure is surfaced via `warn()` + inspectable dead-letter → "honest ops" holds. **Freeze-blocker on B1-1 REJECTED** (see list). Real gap on "bring any model", worth fixing; not a v1 blocker. |
| F2 dead capture-pipeline duplication in public API | AMENDED | P2 | `distill()` and `extractEntities()` = **CONFIRMED dead** (greps below). But the captureTurn/extractCapturePlan reachability reasoning is **wrong**: the auditor says "reachable only via `BujoMemoryStore.capture()`". It is ALSO reachable via `scheduleCapture()`→`captureQueue`→`captureAccepted`→`captureTurn` (store.ts:717,955,803). `scheduleCapture` is a **published optional contract method** (agent-contracts/memory.ts:65) with a live wrapper (memory-retrieval.ts:182). It is dead only because the harness takes the `persistCompletedTurn` branch for any store that implements it (harness.ts:1660-1679) and `BujoMemoryStore` does (store.ts:449). So captureTurn is a **legacy/fallback compat path bypassed for the bujo store**, not simple dead code — deleting it means deciding `scheduleCapture`'s fate on the bujo store. Premise point (orphaned public primitives) stands. |
| F3 `consolidate` name/shape implies cleanup it never does | AMENDED | P2→P3 | Confirmed: consolidate.ts:31 hardcodes `decayed:0, superseded:0, markdownInvalidated:0`; scheduled default 2h (memory-rituals.ts:18 `0 */2 * * *`); no reader of the three fields (the `.decayed` grep hit at cli.ts:112 reads *reflect*'s result, a different type). README has no `consolidate` section (grep: none). BUT the function carries an explicit, accurate docstring ("Projection-only… reported, never folded") and the fields are honestly `0` — so the operator-deception risk is overstated; the real gap is naming + README silence. Legibility nit → P3. |
| F4 recall-evidence is a 344-line hand-rolled mini-parser w/ ad-hoc exceptions | AMENDED | P2→P3 | Confirmed: `atlas` carve-out at recall-evidence.ts:341 and :336; design-intent doc at :1-8. Confirmed it IS the live auto-injection gate (`selectAnswerBearingRecallHits` → `parseDirectFactQuery`→`canonicalName`… used by recall.ts:5, not test-only; `hasAutomaticRecallEvidence`/`automaticRecallEvidenceProfile` ARE test-only). Design is documented, fails **closed** (under-recall), isolated; fix is doc-only (a rationale map). Legibility → P3. |
| F5 Unicode-unsafe truncation in `normalizeCandidateText` | CONFIRMED | P3 | distill.ts:68 `.slice(0,280)` truncates by UTF-16 code unit — confirmed. Reachable on the **live strict path**: reconcile.ts:567/680 call it on reconcile-decision text that is persisted (`text` field in the decision object). Trigger is extremely narrow (astral char straddling index 280), so P3 is right. Prompt/impl unit mismatch (160 code points vs 280 UTF-16) confirmed. |
| F6 `firstFenceBody` returns whole text on unterminated fence | CONFIRMED | P3 | json.ts:223 `end === -1 ? text : …` — confirmed; `text.slice(start)` is the correct fix. Currently **zero production impact** (only reachable via the non-strict/dead path per F2), but a valid one-line correctness fix for the public loose decoder. |

### B1 dead-code table
| symbol | verdict | proof |
|---|---|---|
| `distill()` (distill.ts:22) | CONFIRMED dead | `grep -rn "\bdistill\b" --include="*.ts" packages extras demos scripts website \| grep -v __tests__ \| grep -v /dist/ \| grep -v .test.ts` → only def + `index.ts:101` re-export; every other hit is the *string* "distill" in labels/comments or the unrelated `normalizeCandidate*` imports. Zero callers. |
| `extractEntities()` (entities.ts:103) | CONFIRMED dead | `grep -rn "extractEntities" …` → only def + `index.ts:106` re-export. Zero callers. |
| `captureTurn` (capture.ts:29) | AMENDED | Not simple dead code — see F2. Dead in the shipped bujo host, reachable via the public `scheduleCapture` contract path. `grep -rn "\bcaptureTurn\b"` → def, `index.ts:78`, `store.ts:803` (inside `captureAccepted`, itself fed by the never-invoked-for-bujo queue). |
| `extractCapturePlan` (capture-batch.ts:45) | AMENDED | Same as captureTurn (its only caller is `captureTurnUnlocked` non-strict branch, capture.ts:48). |
| `compareLocated()` (capture-intake.ts:1972) | CONFIRMED dead | `grep -in comparelocated …` → single hit (the definition). No caller. Delete. |

### B1 deprecation notes
- No `@deprecated` items inside B1 scope; the implicit legacy surface (`distill`/`extractEntities` = old two-call "Phase 2" design superseded by batched one-shot) is real. Aligning with F2. Confirmed.

---

## Part B2 — graph & lifecycle

| item | verdict | corrected sev | notes / proof |
|---|---|---|---|
| F1 issue #231 (constant-time non-BuJo replay guard) open & unfixed | CONFIRMED | P2 | `gh issue view 231` → `state: OPEN`. Code path confirmed: `recoverDurableMutationState` runs on **every** `withSerializedBujoMutation` (mutation-lock.ts:89); non-bujo branch calls `replayProjectionDbSnapshot(db)` unconditionally (mutation-lock.ts:172) which scans all rows; `appendHostSummary` for lite/journal → `runAdmittedMutation` (store.ts:442). Real O(rows) per-turn cost on the hot path. Tracked, non-blocking. |
| F2 dead `BujoMemoryStore.reflect()`/`.decay()` | CONFIRMED | P2 | Both `@deprecated` (store.ts:666, :816). Proof-of-death: `grep -rn "\.reflect(" / "\.decay(" --include=*.ts packages extras demos scripts website \| grep -v /dist/ \| grep -v .test.ts` → **zero** hits. CLI uses the standalone `reflect` function (cli.ts:12 import, :100 call), NOT the store method. Live instances: `grep -rn "\.reflect(\|\.decay(\|memory-bujo reflect\|memory-bujo migrate" ~/personal-agent ~/a8c-agents` (excl node_modules) → **zero**. Not on `MemoryStore` contract (memory.ts). Removable now. |
| F3 contradictory "about" edge comments | CONFIRMED | P3 | rebuild.ts:90-91 "deferred to P3+" vs db.ts:1277 "retired". Same fact, opposite framing. Fix to "retired". |
| F4 disproportionate defensive-hardening density (premise tension) | CONFIRMED | (judgment) | Legitimate "lean core" tension, not a defect. Actionable is doc/decision only (record the real threat model: crash-mid-fsync + concurrent-CLI-vs-agent, not multi-tenant). Keep as a freeze-time decision note, non-blocking. |
| F5 `rebuildFromMarkdown` public API, zero internal consumers | CONFIRMED | P3 | `grep -rn rebuildFromMarkdown --include=*.ts packages extras demos scripts \| grep -v /dist/ \| grep -v .test.ts` → def (rebuild.ts:93), `index.ts:13` re-export, one *comment* mention (db.ts:1001). No real caller; product path is `safeRebuildMemoryIndex`. Keep as documented utility; point docstring at the managed path. |

### B2 dead-code / deprecation classifications (spot-verified)
- `reflect()`/`decay()` store methods — CONFIRMED removable now (above).
- `"about"` `MemoryEdgeKind` — CONFIRMED no writer: `grep -rn "'about'\|\"about\"" packages/memory/src` filtered to insert/values/push/write → **no writer**, only type unions + read-side tolerance + CHECK constraint. (Cross-checked with B3 dead-code — consistent.)
- `rebuildFromMarkdown` — CONFIRMED keep-as-utility.
- `reflect.ts` function (module) — CONFIRMED keep: still the `memory-bujo reflect` CLI subcommand (cli.ts:99-112), documented legacy compat, published `bin`. Correct KEEP verdict; distinct from the store method wrappers.
- `legacy-*` family + `explicit-forget.ts` — CONFIRMED keep (load-bearing upgrade/forget paths); B2's live-manifest evidence is a sound method. Not re-derived here (out of removability scope), no contradiction found.

---

## Part B3 — store & search

| item | verdict | corrected sev | notes / proof |
|---|---|---|---|
| F1 schema has no column-evolution path; `CREATE TABLE IF NOT EXISTS` inert on existing DBs | AMENDED | P1→P2 | Evidence confirmed: schema.ts every table uses `IF NOT EXISTS`; no `ALTER TABLE`/`user_version`; migrations run only when `readOnly !== true` (db.ts:120-122). BUT **no defect exists at HEAD** — it's a latent footgun requiring a future contributor to add a column the wrong way; and it is **recoverable** (the SQLite index is rebuildable from canonical markdown). Real guardrail gap worth an issue (header comment + regression test), but not a shipped catastrophe. **Freeze-blocker on A1-1 REJECTED.** |
| F2 dead npm-published search API (`VectorMemoryIndex`/`gatherMemoryChunks`) | AMENDED | P1→P2 | Dead CONFIRMED: `grep -rln "VectorMemoryIndex\|gatherMemoryChunks\|createVectorMemoryIndex\|MemoryChunk" --include=*.ts packages extras demos scripts website \| grep -v /dist/ \| grep -v packages/memory/` → **empty**; only self re-exports (search/index.ts:13-14) + types. Live instances → none. README:198 advertises it as current API. But this is the **same class** as B1-F2 (dead public API, zero runtime impact) which was rated P2 — normalizing to P2 for cross-artifact consistency. **Freeze-blocker on A2-1 REJECTED.** |
| F3 db.ts 2,696 lines mixing 4 concerns | CONFIRMED | P2 | `wc -l` = 2696. Legibility risk on "lean core"; pure-move refactor. Non-blocking. |
| F4 embedding-provider error taxonomy inconsistent (timeout/network unwrapped) | CONFIRMED | P2 | `withTimeout` (embeddings.ts:276-284) has no catch → raw `AbortError`/`TypeError` for the two most common local-outage cases while every other failure is a coded `MemorySearchError`. "Honest ops" gap (uncoded but still surfaced). Non-blocking. |
| F5 endpoint validation inconsistent across 3 providers | CONFIRMED | P3 | `normalizeServiceRoot` (embeddings.ts:256) used only by LM Studio (:105); Ollama/OpenAI strip trailing slash only. Operator-supplied config, low exploitability. |
| F6 `mkdirSync` runs even when `readOnly:true` | CONFIRMED | P3 | db.ts:107-109 `mkdirSync` before the readOnly branch (:110). A read-only open against a missing parent dir creates the tree then throws. Small contract leak; one-line guard. |

### B3 dead-code / deprecation (spot-verified)
- `vector-index.ts`+`chunks.ts` — CONFIRMED dead (= F2).
- `recency` / `decayGamma` / `DEFAULT_DECAY_GAMMA` inert fields — CONFIRMED intentionally inert, honestly commented, not exposed via any agent-app config surface. Keep through v1. Not a removal claim.
- `"about"` `MemoryEdgeKind` — CONFIRMED (cross-check with B2, no writer).
- `MemoryDb.applyDecay()` — CONFIRMED `@deprecated` no-op; removable-now internally (spy-tested to prove non-invocation), kept only as external-package compat. Distinct from `BujoMemoryStore.decay()` in B2 (different symbol). Correct classification.

---

## Confirmed freeze-blockers
**None.** No finding in this cluster blocks a v1 DoD clause, ships a secret/license/
correctness catastrophe, or makes "honest ops" false. The three subsystems are
honest end-to-end: capture fails closed into an inspectable dead-letter with the
deterministic summary preserved; the durability/replay machinery is exceptional;
and the store swallows no errors (the one uncoded case, F4, still throws).

## Rejected freeze-blockers (second key withheld)
- **B1-1 (F1 — strict-capture fence tolerance / Ollama JSON mode).** Fails closed: deterministic host summary is always written (no data loss), degradation is surfaced via `warn()` + a durable dead-letter. Honest ops holds. → real P1-ish gap, file as an issue, **not** a v1 blocker.
- **A1-1 (B3-F1 — schema column-migration guardrail).** Latent footgun, **no defect at HEAD**; recoverable by rebuilding the index from canonical markdown. → P2 issue (header comment + regression test), not a blocker.
- **A2-1 (B3-F2 — dead published search vector-index API).** Dead code with zero runtime impact; not a secret/correctness/honesty issue. → P2 cleanup (delete or `@deprecated` + README), not a blocker.

## Cross-artifact contradictions / notes
- **Severity inconsistency for "dead public API":** B1-F2 rated it P2 (distill/extractEntities); B3-F2 rated the same class P1 (VectorMemoryIndex). Normalized both to P2.
- **"about" edge kind** is independently flagged by B2-F3 and B3 dead-code; the two are complementary and consistent (no writer anywhere; comment drift between rebuild.ts and db.ts). Consolidate into one fix.
- **ollama-llm.ts** is claimed in B1's scope (F1) and read as a catch-all by B2; B2 correctly defers findings to B1. No contradiction.
- B2's `decay()` (BujoMemoryStore method) and B3's `applyDecay()` (MemoryDb method) are **different symbols** — not double-counted.

## NEW findings (discovered while re-reading code)
- **NEW-1 [P2, dead code] — the entire best-effort capture queue is created-but-never-fed for a bujo store.** `initializeCaptureQueue()` runs for every writable bujo store (store.ts:331), building `captureQueue`/`CaptureJob`/`activeCaptureController` and the `captureAccepted`→`captureTurn` processor (store.ts:945-964). Its only feeder is `scheduleCapture` (store.ts:717), which the shipped harness invokes **only** in the `persistCompletedTurn === undefined` fallback branch (harness.ts:1677) — and `BujoMemoryStore` always defines `persistCompletedTurn` (store.ts:449). So for the bujo store the queue, `captureAccepted`, `captureTurn`, `extractCapturePlan`, `distill`, `extractEntities`, and `parseJsonLoose` form one dead-in-shipped-product subsystem reachable only through the public `scheduleCapture`/`capture()` surface. This enlarges B1-2/B1-3's cleanup scope beyond the two functions the auditor named, and is the mechanical reason B1-F2's "reachable only via `BujoMemoryStore.capture()`" reasoning is incomplete. Proof: greps in this doc (scheduleCapture callers, `.capture(` callers, initializeCaptureQueue call site store.ts:331, harness.ts:1660-1679).
